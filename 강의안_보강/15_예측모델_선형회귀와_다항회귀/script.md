# [15차시] 예측 모델 - 선형/다항회귀 - 강사 스크립트

## 📋 수업 개요

| 항목 | 내용 |
|------|------|
| 차시 | 15차시 |
| 주제 | 예측 모델 - 선형/다항회귀 |
| 시간 | 30분 (이론 15분 + 실습 13분 + 정리 2분) |
| 학습 목표 | 회귀 문제 이해, 선형회귀, 다항회귀 적용 |

---

## 🎯 학습 목표

1. 회귀 문제의 개념을 이해한다
2. 선형회귀 모델을 사용한다
3. 다항회귀로 비선형 관계를 학습한다

---

## 🕐 시간 배분

| 구간 | 시간 | 내용 |
|------|------|------|
| 도입 | 2분 | 복습 및 학습목표 |
| Part 1 | 4분 | 회귀 문제 이해 |
| Part 2 | 6분 | 선형회귀 |
| Part 3 | 5분 | 다항회귀 |
| 실습 | 11분 | 생산량 예측 |
| 정리 | 2분 | 요약 및 다음 차시 예고 |

---

## 📝 상세 스크립트

### 도입부 (2분)

#### 슬라이드 1-2: 복습

> "지금까지 분류 문제를 배웠습니다. 의사결정나무, 랜덤포레스트로 불량인지 정상인지를 예측했죠."

> "오늘부터는 회귀 문제를 다룹니다. 회귀는 '숫자'를 예측합니다. 생산량이 몇 개인지, 불량률이 몇 %인지 같은 연속적인 값을요."

---

#### 슬라이드 3-4: 학습 목표

> "오늘의 학습 목표는 세 가지입니다. 회귀가 뭔지 이해하고, 가장 기본적인 선형회귀를 배우고, 곡선 관계를 위한 다항회귀까지 다룹니다."

---

### Part 1: 회귀 문제 이해 (4분)

#### 슬라이드 5-7: 분류 vs 회귀

> "분류와 회귀의 차이는 간단합니다. 출력이 뭔가요?"

> "분류는 범주입니다. 정상이냐 불량이냐, A등급이냐 B등급이냐."

> "회귀는 숫자입니다. 생산량 1,247개, 불량률 3.2%, 남은 수명 87일."

> "질문 형태로 구분하면, '~인가요?'는 분류, '얼마나?'는 회귀입니다."

---

#### 슬라이드 8-9: 평가 지표

> "분류는 정확도로 평가했죠. 회귀는 어떻게 평가할까요?"

> "MSE는 평균 제곱 오차입니다. 실제값과 예측값의 차이를 제곱해서 평균낸 거예요. 작을수록 좋습니다."

> "R² 점수는 결정계수라고 합니다. 모델이 데이터를 얼마나 잘 설명하는지를 나타내요. 1에 가까울수록 좋고, 0.8 이상이면 좋은 모델입니다."

---

### Part 2: 선형회귀 (6분)

#### 슬라이드 10-11: 선형회귀란

> "선형회귀는 가장 기본적인 회귀 모델입니다. 입력과 출력 사이의 직선 관계를 학습합니다."

> "수식으로 표현하면 y = wx + b입니다. w는 기울기, b는 절편이에요."

---

#### 슬라이드 12-14: 기울기와 절편

> "기울기 w가 뭘 의미할까요? x가 1 증가할 때 y가 얼마나 변하는지입니다."

> "예를 들어 w = -3이면, 온도가 1도 올라가면 생산량이 3개 줄어든다는 뜻이에요."

> "절편 b는 x가 0일 때 y 값입니다. 기준점이라고 생각하면 됩니다."

---

#### 슬라이드 15-17: 다중 선형회귀

> "특성이 여러 개면요? 각 특성마다 계수가 붙습니다."

> "생산량 = 5×속도 - 3×온도 + 2×습도 + 1000 이런 식으로요."

> "각 계수를 보면 어떤 특성이 얼마나 영향을 주는지 알 수 있습니다. 해석이 쉬운 게 선형회귀의 장점이에요."

---

#### 슬라이드 18-20: sklearn LinearRegression

> "sklearn에서는 LinearRegression을 사용합니다."

```python
from sklearn.linear_model import LinearRegression
model = LinearRegression()
model.fit(X_train, y_train)
y_pred = model.predict(X_test)
```

> "학습 후에 model.coef_로 계수를, model.intercept_로 절편을 확인할 수 있습니다."

---

### Part 3: 다항회귀 (5분)

#### 슬라이드 21-23: 비선형 관계

> "선형회귀는 직선입니다. 근데 데이터가 곡선 형태면요?"

> "직선으로는 잘 맞지 않겠죠. 이때 다항회귀를 사용합니다."

---

#### 슬라이드 24-26: 다항회귀란

> "다항회귀는 특성의 거듭제곱을 추가합니다. x뿐 아니라 x², x³ 같은 항을 넣는 거죠."

> "2차 다항회귀는 y = w₁x + w₂x² + b 형태입니다. 포물선 모양의 곡선을 만들 수 있어요."

---

#### 슬라이드 27-29: PolynomialFeatures

> "sklearn에서는 PolynomialFeatures로 다항 특성을 만듭니다."

```python
from sklearn.preprocessing import PolynomialFeatures
from sklearn.pipeline import Pipeline

poly_model = Pipeline([
    ('poly', PolynomialFeatures(degree=2)),
    ('linear', LinearRegression())
])
```

> "Pipeline으로 다항 특성 생성과 선형회귀를 연결합니다."

---

#### 슬라이드 30-32: 차수 선택과 과대적합

> "degree를 높이면 더 복잡한 곡선을 만들 수 있습니다. 하지만 주의하세요!"

> "degree가 너무 높으면 과대적합됩니다. 학습 데이터에는 잘 맞지만 새 데이터에서 엉망이 되죠."

> "보통 degree 2~3으로 시작해서, 학습 점수와 테스트 점수를 비교하며 선택합니다."

---

### 실습편 (11분)

#### 슬라이드 33-35: 데이터 생성

> "제조 데이터로 생산량 예측 실습을 합니다."

```python
import numpy as np
import pandas as pd

np.random.seed(42)
n = 500

df = pd.DataFrame({
    'temperature': np.random.normal(85, 5, n),
    'humidity': np.random.normal(50, 10, n),
    'speed': np.random.normal(100, 15, n),
})

# 비선형 관계 포함
df['production'] = (
    1000 + 5*df['speed'] - 3*df['temperature']
    - 0.05*df['temperature']**2 + np.random.normal(0, 50, n)
)
```

> "일부러 온도의 제곱항을 넣어서 비선형 관계를 만들었습니다."

---

#### 슬라이드 36-38: 선형회귀 적용

> "먼저 선형회귀를 적용합니다."

```python
from sklearn.linear_model import LinearRegression
from sklearn.model_selection import train_test_split

X = df[['temperature', 'humidity', 'speed']]
y = df['production']

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)

linear_model = LinearRegression()
linear_model.fit(X_train, y_train)
print(f"R² 점수: {linear_model.score(X_test, y_test):.3f}")
```

---

#### 슬라이드 39-41: 다항회귀 비교

> "이번에는 다항회귀를 적용합니다."

```python
from sklearn.preprocessing import PolynomialFeatures
from sklearn.pipeline import Pipeline

poly_model = Pipeline([
    ('poly', PolynomialFeatures(degree=2)),
    ('linear', LinearRegression())
])

poly_model.fit(X_train, y_train)
print(f"다항회귀 R²: {poly_model.score(X_test, y_test):.3f}")
```

> "비선형 관계가 있으니까 다항회귀가 더 높은 점수를 보일 겁니다."

---

#### 슬라이드 42-44: 차수별 비교

> "차수를 바꿔가며 실험합니다."

```python
for deg in range(1, 6):
    poly = Pipeline([
        ('poly', PolynomialFeatures(degree=deg)),
        ('linear', LinearRegression())
    ])
    poly.fit(X_train, y_train)
    train_score = poly.score(X_train, y_train)
    test_score = poly.score(X_test, y_test)
    print(f"degree={deg}: 학습 {train_score:.3f}, 테스트 {test_score:.3f}")
```

> "차수가 너무 높으면 학습 점수는 높은데 테스트 점수가 떨어집니다. 과대적합이에요."

---

### 정리 (2분)

#### 슬라이드 45-46: 핵심 정리

> "오늘 배운 내용을 정리합니다."

> "회귀는 연속적인 숫자를 예측하고, MSE나 R² 점수로 평가합니다."

> "선형회귀는 y = wx + b 형태의 직선 관계를 학습하고, 계수 해석이 쉽습니다."

> "다항회귀는 곡선 관계를 학습하지만, 차수가 높으면 과대적합에 주의해야 합니다."

---

#### 슬라이드 47-48: 다음 차시 예고

> "다음 시간에는 모델 평가를 더 정확하게 하는 방법을 배웁니다. 교차검증이라는 기법으로 모델 성능을 더 신뢰성 있게 측정합니다."

> "오늘 수업 마무리합니다. 수고하셨습니다!"

---

## ❓ 예상 질문 및 답변

### Q1: R² 점수가 음수가 나올 수도 있나요?

> "네, 가능합니다. R² 점수가 0이면 평균으로 예측하는 것과 같은 수준이고, 음수면 평균보다 못한 겁니다. 모델이 데이터에 전혀 맞지 않다는 뜻이에요."

### Q2: MSE와 RMSE 중 어떤 걸 봐야 하나요?

> "RMSE가 해석하기 쉽습니다. MSE는 제곱 단위라서 직관적이지 않지만, RMSE는 원래 단위와 같아요. RMSE가 50이면 평균적으로 50 정도 오차가 난다고 해석합니다."

### Q3: 다항회귀에서 degree는 보통 몇으로 하나요?

> "2~3부터 시작하세요. 데이터가 아주 복잡한 관계가 아니라면 2차로 충분한 경우가 많습니다. 차수를 높이기 전에 과대적합 여부를 확인하세요."

### Q4: 회귀에도 랜덤포레스트를 쓸 수 있나요?

> "네! RandomForestRegressor가 있습니다. 복잡한 비선형 관계도 잘 학습하고, 과대적합에 강합니다. 다음 차시에서 다양한 회귀 모델을 비교해볼 수 있습니다."

---

## 📚 참고 자료

### 공식 문서
- [sklearn LinearRegression](https://scikit-learn.org/stable/modules/generated/sklearn.linear_model.LinearRegression.html)
- [sklearn PolynomialFeatures](https://scikit-learn.org/stable/modules/generated/sklearn.preprocessing.PolynomialFeatures.html)

### 관련 차시
- 15차시: 모델 평가와 반복 검증
- 16차시: 모델 설정값 최적화

---

## ✅ 체크리스트

수업 전:
- [ ] sklearn 설치 확인
- [ ] 예제 코드 테스트

수업 중:
- [ ] 분류 vs 회귀 차이 설명
- [ ] R² 점수 해석 방법 설명
- [ ] 선형회귀 계수 해석
- [ ] 다항회귀 과대적합 경고

수업 후:
- [ ] 실습 코드 배포
- [ ] 교차검증 예고
