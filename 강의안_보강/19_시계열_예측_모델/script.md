# [19차시] 시계열 예측 모델 - 강사 스크립트

## 📋 수업 개요

| 항목 | 내용 |
|------|------|
| 차시 | 19차시 |
| 주제 | 시계열 예측 모델 |
| 시간 | 30분 (이론 15분 + 실습 13분 + 정리 2분) |
| 학습 목표 | 특성 엔지니어링, ML 예측, 평가 |

---

## 🎯 학습 목표

1. 시계열 특성 엔지니어링을 수행한다
2. ML 모델로 시계열을 예측한다
3. 시계열 예측을 평가한다

---

## 🕐 시간 배분

| 구간 | 시간 | 내용 |
|------|------|------|
| 도입 | 2분 | 복습 및 학습목표 |
| 대주제 1 | 5분 | 특성 엔지니어링 |
| 대주제 2 | 5분 | ML 모델 적용 |
| 대주제 3 | 5분 | 예측 평가 |
| 실습 | 11분 | 생산량 예측 |
| 정리 | 2분 | 요약 및 다음 차시 예고 |

---

## 📝 상세 스크립트

### 도입부 (2분)

#### 슬라이드 1-2: 복습

> "지난 시간에 시계열 데이터의 특성과 전처리 기법을 배웠습니다. resample, rolling, shift를 다뤘죠."

> "오늘은 그 특성들을 활용해서 실제로 예측 모델을 만들어봅니다. 내일 생산량이 얼마일지 예측하는 거예요."

---

#### 슬라이드 3-4: 학습 목표

> "오늘의 학습 목표는 세 가지입니다. 시계열 특성 엔지니어링을 더 깊이 다루고, ML 모델로 시계열을 예측하고, 예측 결과를 평가하는 방법을 배웁니다."

---

### 대주제 1: 시계열 특성 엔지니어링 (5분)

#### 슬라이드 5-7: 특성 엔지니어링이란

> "특성 엔지니어링은 원본 데이터에서 예측에 유용한 특성을 만드는 작업입니다."

> "시계열에서는 단순히 과거 값만 쓰면 부족해요. 패턴을 담은 특성을 만들어야 합니다."

> "종류로는 날짜 특성, Lag 특성, Rolling 특성, 변화 특성, 외부 특성이 있습니다."

---

#### 슬라이드 8-10: 날짜 특성

> "날짜에서 추출하는 특성입니다."

```python
df['hour'] = df.index.hour
df['dayofweek'] = df.index.dayofweek
df['is_weekend'] = (df.index.dayofweek >= 5).astype(int)
```

> "이렇게 하면 모델이 '오전에 생산량이 높다', '월요일은 불량률이 높다' 같은 패턴을 학습할 수 있습니다."

---

#### 슬라이드 11-12: 주기적 인코딩

> "한 가지 문제가 있어요. hour는 0, 1, ..., 23인데, 23시와 0시는 실제로 1시간 차이지만 숫자로는 23 차이입니다."

> "이걸 해결하려면 사인/코사인으로 인코딩합니다. 원형으로 표현되니까 23시와 0시가 가깝게 됩니다."

---

#### 슬라이드 13-15: Lag와 Rolling 특성

> "Lag 특성은 지난 시간에 배운 shift를 사용합니다. 1시간 전, 24시간 전, 7일 전 값을 특성으로 씁니다."

```python
df['lag_1'] = df['production'].shift(1)
df['lag_24'] = df['production'].shift(24)
```

> "Rolling 특성은 이동평균, 이동표준편차 등입니다. 반드시 shift(1)을 먼저 해서 미래 누출을 막으세요!"

```python
df['ma_24'] = df['production'].shift(1).rolling(24).mean()
```

---

### 대주제 2: ML 모델로 시계열 예측 (5분)

#### 슬라이드 16-18: 시계열 예측 접근법

> "시계열 예측에는 두 가지 접근법이 있습니다."

> "전통적 방법은 ARIMA 같은 시계열 전용 모델입니다. 오늘 배울 건 ML 기반 방법이에요."

> "특성 엔지니어링을 잘 하면 RandomForest, XGBoost 같은 일반 ML 모델로도 시계열 예측을 잘 할 수 있습니다."

---

#### 슬라이드 19-21: RandomForest 적용

> "RandomForest로 시계열 예측하는 흐름을 보겠습니다."

```python
from sklearn.ensemble import RandomForestRegressor

model = RandomForestRegressor(n_estimators=100, random_state=42)
model.fit(X_train, y_train)
y_pred = model.predict(X_test)
```

> "일반적인 ML과 똑같죠? 차이점은 특성이 시계열 특성이라는 것, 그리고 시간 기준으로 분할한다는 것입니다."

---

#### 슬라이드 22-24: 왜 RandomForest가 좋은가

> "RandomForest가 시계열에 좋은 이유는 비선형 관계를 잘 학습하고, 특성 중요도를 자동으로 알려주고, 과적합에 강하기 때문입니다."

> "단점은 시간의 순차적 의존성을 직접 학습하지 못한다는 건데, 이건 Lag 특성으로 보완합니다."

---

#### 슬라이드 25-26: TimeSeriesSplit

> "교차검증할 때 주의할 점이 있습니다. 일반 KFold는 랜덤 분할이라 시계열에 안 맞아요."

> "TimeSeriesSplit을 쓰면 항상 과거로 학습하고 미래로 검증합니다."

```python
from sklearn.model_selection import TimeSeriesSplit
tscv = TimeSeriesSplit(n_splits=5)
```

---

### 대주제 3: 시계열 예측 평가 (5분)

#### 슬라이드 27-29: 평가 지표

> "시계열 예측 평가에 쓰는 지표들입니다."

> "MAE는 평균 절대 오차입니다. 해석이 쉬워요. MAE가 50이면 평균적으로 50 정도 틀린다는 뜻입니다."

> "RMSE는 큰 오차에 더 큰 패널티를 줍니다. 이상치에 민감해요."

> "MAPE는 퍼센트로 표현합니다. MAPE 5%면 평균 5% 오차입니다. 비즈니스 보고에 좋아요."

---

#### 슬라이드 30-32: 지표 계산

```python
from sklearn.metrics import mean_absolute_error, mean_squared_error

mae = mean_absolute_error(y_test, y_pred)
rmse = np.sqrt(mean_squared_error(y_test, y_pred))
mape = np.mean(np.abs((y_test - y_pred) / y_test)) * 100
```

> "sklearn에서 MAE, MSE는 제공하지만 MAPE는 직접 계산해야 합니다."

---

#### 슬라이드 33-35: 시각적 평가

> "숫자 지표만 보면 안 됩니다. 반드시 그래프를 그려보세요."

> "실제 vs 예측 플롯을 그려서 전체적인 흐름을 봅니다. 특정 구간에서 많이 틀리는지 확인해요."

> "잔차 분석도 중요합니다. 잔차가 0 주변에 고르게 분포해야 좋은 모델입니다. 시간에 따른 패턴이 남아 있으면 모델이 뭔가를 놓친 겁니다."

---

### 실습편 (11분)

#### 슬라이드 36-38: 데이터 생성

> "30일간 시간별 생산량 데이터를 만듭니다."

```python
np.random.seed(42)
dates = pd.date_range('2025-01-01', periods=24*30, freq='H')
hours = np.array([d.hour for d in dates])

df = pd.DataFrame({
    'timestamp': dates,
    'production': 1000 + 30*np.sin(2*np.pi*hours/24) + \
                  np.random.normal(0, 50, len(dates))
})
df = df.set_index('timestamp')
```

> "오전에 생산량이 높은 패턴을 넣었습니다."

---

#### 슬라이드 39-41: 특성 생성

> "특성을 만듭니다. 날짜, Lag, Rolling 특성을 다 만들어요."

```python
# 날짜 특성
df['hour'] = df.index.hour
df['dayofweek'] = df.index.dayofweek

# Lag 특성 (shift!)
df['lag_1'] = df['production'].shift(1)
df['lag_24'] = df['production'].shift(24)

# Rolling 특성 (shift 먼저!)
df['ma_24'] = df['production'].shift(1).rolling(24).mean()
```

> "shift를 안 하면 미래 정보가 포함돼서 결과가 비현실적으로 좋아집니다. 실전에서 실패해요."

---

#### 슬라이드 42-44: 학습 및 예측

> "시간 기준으로 분할하고 모델을 학습합니다."

```python
split_date = '2025-01-25'
X_train = features[:split_date]
y_train = target[:split_date]
X_test = features[split_date:]
y_test = target[split_date:]

model = RandomForestRegressor(n_estimators=100, random_state=42)
model.fit(X_train, y_train)
y_pred = model.predict(X_test)
```

---

#### 슬라이드 45-47: 평가

> "MAE, RMSE, MAPE를 계산합니다."

```python
mae = mean_absolute_error(y_test, y_pred)
rmse = np.sqrt(mean_squared_error(y_test, y_pred))
mape = np.mean(np.abs((y_test - y_pred) / y_test)) * 100

print(f"MAE: {mae:.2f}")
print(f"RMSE: {rmse:.2f}")
print(f"MAPE: {mape:.1f}%")
```

> "MAPE가 5% 이하면 꽤 좋은 예측입니다. 10% 이하도 실용적이에요."

---

#### 슬라이드 48-50: 특성 중요도

> "어떤 특성이 중요한지 확인합니다."

```python
importances = model.feature_importances_
```

> "보통 lag_1, lag_24 같은 Lag 특성이 가장 중요하게 나옵니다. 어제 생산량이 오늘 생산량을 가장 잘 예측한다는 뜻이죠."

---

### 정리 (2분)

#### 슬라이드 51-52: 핵심 정리

> "오늘 배운 내용을 정리합니다."

> "특성 엔지니어링에서 날짜, Lag, Rolling 특성을 만들고, shift(1)로 미래 누출을 막습니다."

> "RandomForest 같은 ML 모델로 시계열 예측이 가능합니다. TimeSeriesSplit으로 교차검증하세요."

> "평가는 MAE, RMSE, MAPE를 사용하고, 반드시 실제 vs 예측 그래프를 그려보세요."

---

#### 슬라이드 53-54: 다음 차시 예고

> "다음 시간에는 딥러닝을 시작합니다. 인공 뉴런과 신경망의 기초를 배웁니다."

> "오늘 수업 마무리합니다. 수고하셨습니다!"

---

## ❓ 예상 질문 및 답변

### Q1: ARIMA와 ML 중 어떤 게 더 좋나요?

> "상황에 따라 다릅니다. ARIMA는 단변량 시계열에 특화되어 있고, ML은 다양한 특성을 활용할 수 있습니다. 외부 변수(날씨, 휴일 등)가 중요하면 ML이 유리합니다."

### Q2: 특성을 많이 만들면 좋은가요?

> "많다고 좋은 건 아닙니다. 관련 없는 특성이 많으면 오히려 과적합됩니다. 특성 중요도를 보고 중요하지 않은 특성은 제거하세요."

### Q3: MAPE가 0으로 나누기 오류가 나요

> "y_test에 0이 있으면 MAPE를 계산할 수 없습니다. 이런 경우 MAE나 RMSE를 쓰거나, 0인 행을 제외하고 MAPE를 계산하세요."

### Q4: 며칠 후 예측은 어떻게 하나요?

> "다단계 예측이 필요합니다. Recursive 방식은 t+1 예측값을 t+2 입력으로 사용합니다. 오차가 누적되는 단점이 있어요. Direct 방식은 각 horizon별로 별도 모델을 만듭니다."

### Q5: 특성 중요도에서 lag_1이 압도적인데 다른 특성은 필요 없나요?

> "lag_1만으로는 장기 패턴이나 계절성을 못 잡습니다. lag_24, lag_168 같은 특성이 주간/월간 패턴을 잡아줍니다. 특성 중요도가 낮아도 성능에 기여할 수 있어요."

---

## 📚 참고 자료

### 공식 문서
- [sklearn TimeSeriesSplit](https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.TimeSeriesSplit.html)
- [sklearn RandomForestRegressor](https://scikit-learn.org/stable/modules/generated/sklearn.ensemble.RandomForestRegressor.html)

### 관련 차시
- 17차시: 시계열 데이터 기초
- 19차시: 딥러닝 입문

---

## ✅ 체크리스트

수업 전:
- [ ] 시계열 데이터 예제 준비
- [ ] RandomForestRegressor 테스트
- [ ] 평가 지표 계산 확인

수업 중:
- [ ] shift(1) 필수 강조
- [ ] 시간 기준 분할 설명
- [ ] 특성 중요도 해석
- [ ] 시각적 평가 중요성

수업 후:
- [ ] 실습 코드 배포
- [ ] 딥러닝 예고
