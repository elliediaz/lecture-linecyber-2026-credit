# [17차시] 모델 설정값 최적화 - 강사 스크립트

## 📋 수업 개요

| 항목 | 내용 |
|------|------|
| 차시 | 17차시 |
| 주제 | 모델 설정값 최적화 |
| 시간 | 30분 (이론 15분 + 실습 13분 + 정리 2분) |
| 학습 목표 | 하이퍼파라미터 개념, GridSearchCV, RandomizedSearchCV |

---

## 🎯 학습 목표

1. 하이퍼파라미터의 개념을 이해한다
2. GridSearchCV로 최적값을 탐색한다
3. RandomizedSearchCV로 효율적 탐색을 수행한다

---

## 🕐 시간 배분

| 구간 | 시간 | 내용 |
|------|------|------|
| 도입 | 2분 | 복습 및 학습목표 |
| 대주제 1 | 4분 | 하이퍼파라미터 개념 |
| 대주제 2 | 6분 | GridSearchCV |
| 대주제 3 | 5분 | RandomizedSearchCV |
| 실습 | 11분 | 모델 최적화 실습 |
| 정리 | 2분 | 요약 및 다음 차시 예고 |

---

## 📝 상세 스크립트

### 도입부 (2분)

#### 슬라이드 1-2: 복습

> "지난 시간에 교차검증과 평가 지표를 배웠습니다. 모델 성능을 정확하게 측정하는 방법을 알았죠."

> "그런데 질문이 있습니다. max_depth는 몇으로 해야 할까요? n_estimators는? 이런 값들을 어떻게 정할까요?"

> "오늘은 이런 '설정값'들을 자동으로 찾는 방법을 배웁니다."

---

#### 슬라이드 3-4: 학습 목표

> "오늘의 학습 목표는 세 가지입니다. 하이퍼파라미터가 뭔지 이해하고, GridSearchCV로 모든 조합을 탐색하고, RandomizedSearchCV로 효율적으로 탐색하는 방법을 배웁니다."

---

### 대주제 1: 하이퍼파라미터의 개념 (4분)

#### 슬라이드 5-7: 파라미터 vs 하이퍼파라미터

> "먼저 용어를 정리합니다. '파라미터'와 '하이퍼파라미터'는 다릅니다."

> "파라미터는 모델이 데이터를 보고 스스로 학습하는 값입니다. 예를 들어 선형회귀의 가중치(w)나 절편(b)은 우리가 정하는 게 아니라 학습 과정에서 자동으로 결정됩니다."

> "반면 하이퍼파라미터는 사람이 미리 정해주는 값입니다. max_depth, n_estimators 같은 것들이요. 학습 전에 설정해야 하고, 이 값에 따라 모델 성능이 크게 달라집니다."

---

#### 슬라이드 8-9: 하이퍼파라미터 예시

> "어떤 하이퍼파라미터가 있을까요?"

**[표 설명]**
> "의사결정나무에는 max_depth(트리 깊이), min_samples_split(분할 최소 샘플)이 있습니다."

> "랜덤포레스트에는 n_estimators(트리 개수), max_features(특성 선택 개수)가 있죠."

> "이 값들을 어떻게 정하느냐에 따라 0.7이 될 수도, 0.9가 될 수도 있습니다."

---

#### 슬라이드 10-11: 왜 최적화가 필요한가

> "실험을 해보겠습니다. max_depth만 바꿔보면..."

```python
# max_depth=2: 너무 얕아서 과소적합 → 0.72
# max_depth=10: 적절 → 0.89
# max_depth=None: 너무 깊어서 과대적합 → 0.85
```

> "같은 모델인데 하이퍼파라미터에 따라 점수가 0.72부터 0.89까지 차이납니다. 최적값을 찾는 게 중요하겠죠?"

---

#### 슬라이드 12-13: 수동 탐색의 한계

> "그럼 일일이 해보면 되지 않나요? 문제가 있습니다."

> "첫째, 시간이 너무 많이 걸립니다. 둘째, 파라미터가 여러 개면 조합이 폭발합니다."

> "예를 들어 3개 파라미터에 각각 5개 값을 테스트하면, 5×5×5 = 125가지 조합입니다. 교차검증 5회를 하면 625번 학습해야 해요. 이걸 수동으로 하긴 힘들죠."

---

### 대주제 2: GridSearchCV로 최적값 탐색 (6분)

#### 슬라이드 14-16: GridSearchCV란

> "해결책이 GridSearchCV입니다. Grid는 격자, Search는 탐색, CV는 교차검증입니다."

> "원리는 간단합니다. 테스트하고 싶은 값들을 지정하면, 모든 조합을 자동으로 만들어서 교차검증을 돌립니다. 그리고 가장 점수가 높은 조합을 알려줍니다."

---

#### 슬라이드 17-19: GridSearchCV 시각화

> "시각적으로 보면 이렇습니다."

```
max_depth: [3, 5, 7]
min_samples_split: [2, 5, 10]

→ 3×3 = 9개 격자점을 모두 테스트
```

> "각 격자점에서 5-Fold 교차검증을 하니까, 총 45번 학습합니다. 이걸 자동으로 해주는 거예요."

---

#### 슬라이드 20-22: GridSearchCV 사용법

> "코드로 보겠습니다."

```python
from sklearn.model_selection import GridSearchCV

# 탐색 범위 정의
param_grid = {
    'max_depth': [3, 5, 7, 10],
    'min_samples_split': [2, 5, 10]
}

# GridSearchCV 생성
grid_search = GridSearchCV(
    estimator=DecisionTreeClassifier(),
    param_grid=param_grid,
    cv=5,
    scoring='accuracy'
)

# 실행
grid_search.fit(X_train, y_train)
```

> "param_grid에 딕셔너리로 탐색할 값들을 지정합니다. fit하면 모든 조합을 테스트합니다."

---

#### 슬라이드 23-25: GridSearchCV 결과 확인

> "결과는 이렇게 확인합니다."

```python
print(grid_search.best_params_)   # 최적 파라미터
print(grid_search.best_score_)    # 최고 점수
best_model = grid_search.best_estimator_  # 최적 모델
```

> "best_params_는 가장 좋았던 파라미터 조합입니다. best_estimator_는 그 파라미터로 학습된 모델이에요. 바로 예측에 사용할 수 있습니다."

---

#### 슬라이드 26-28: scoring 옵션

> "scoring 옵션으로 어떤 지표로 최적화할지 정합니다."

| 분류 | 회귀 |
|------|------|
| 'accuracy' | 'r2' |
| 'f1' | 'neg_mean_squared_error' |
| 'recall' | 'neg_mean_absolute_error' |

> "불량 검출처럼 재현율이 중요하면 scoring='recall'로 설정하세요. 회귀에서 MSE를 쓰려면 neg_mean_squared_error를 씁니다. 앞에 neg가 붙는 건 sklearn이 점수를 최대화하는 방향으로 동작하기 때문이에요."

---

#### 슬라이드 29-30: GridSearchCV의 한계

> "GridSearchCV는 철저하지만 느립니다. 파라미터가 많아지면 조합이 기하급수적으로 늘어납니다."

> "4개 파라미터에 각 10개 값이면 10,000 조합입니다. 5-Fold면 50,000번 학습... 며칠 걸릴 수도 있어요."

> "그래서 더 효율적인 방법이 필요합니다."

---

### 대주제 3: RandomizedSearchCV로 효율적 탐색 (5분)

#### 슬라이드 31-33: RandomizedSearchCV란

> "RandomizedSearchCV는 모든 조합을 테스트하지 않고 랜덤으로 일부만 샘플링합니다."

> "n_iter=50이면 50개 조합만 테스트합니다. 10,000개 중 50개만 보는 거죠. 훨씬 빠릅니다."

> "놀랍게도 대부분의 경우 Grid Search와 비슷하거나 더 좋은 결과를 얻습니다. 왜냐하면 중요한 파라미터는 어떤 조합에서든 좋은 값이 되기 때문이에요."

---

#### 슬라이드 34-36: Grid vs Random 비교

> "그림으로 비교하면..."

```
Grid: ●-●-●-●-●    Random: ○
      ●-●-●-●-●           ○  ○
      ●-●-●-●-●         ○      ○
→ 25개 전부 탐색       → 10개만 랜덤 샘플링
```

> "Grid는 격자의 모든 점을 봅니다. Random은 랜덤하게 몇 개만 봅니다. 운이 좋으면 최적점 근처를 찍을 수 있어요."

---

#### 슬라이드 37-39: RandomizedSearchCV 사용법

> "코드를 보겠습니다."

```python
from sklearn.model_selection import RandomizedSearchCV
from scipy.stats import randint

param_distributions = {
    'max_depth': randint(3, 20),       # 3~19 정수
    'min_samples_split': randint(2, 20) # 2~19 정수
}

random_search = RandomizedSearchCV(
    estimator=model,
    param_distributions=param_distributions,
    n_iter=50,  # 50개만 탐색
    cv=5
)
```

> "param_distributions에 분포를 지정합니다. randint(3, 20)은 3부터 19까지 정수를 랜덤하게 샘플링합니다."

---

#### 슬라이드 40-42: 분포 함수

> "scipy.stats에서 다양한 분포를 가져다 쓸 수 있습니다."

| 함수 | 용도 |
|------|------|
| `randint(a, b)` | 정수 파라미터 |
| `uniform(a, b)` | 실수 파라미터 |
| `loguniform(a, b)` | 로그 스케일 |

> "learning_rate처럼 0.0001~0.1 범위면 loguniform을 씁니다. 작은 값 쪽도 충분히 탐색되도록요."

---

#### 슬라이드 43-44: 실무 전략 - 2단계 탐색

> "실무에서는 이렇게 합니다."

> "1단계: RandomizedSearchCV로 넓은 범위를 빠르게 훑습니다. 좋은 영역을 파악해요."

> "2단계: 그 영역 주변으로 GridSearchCV를 세밀하게 돌립니다."

> "예를 들어 Random으로 max_depth=8이 좋다고 나왔으면, Grid로 [6, 7, 8, 9, 10]만 정밀 탐색하는 거죠."

---

### 실습편 (11분)

#### 슬라이드 45-47: 실습 환경 준비

> "제조 불량 예측 모델을 최적화합니다."

```python
import numpy as np
import pandas as pd
from sklearn.model_selection import train_test_split

np.random.seed(42)
n = 500

df = pd.DataFrame({
    'temperature': np.random.normal(85, 5, n),
    'pressure': np.random.normal(100, 10, n),
    'vibration': np.random.normal(0.5, 0.1, n),
    'humidity': np.random.normal(50, 10, n)
})

df['defect'] = ((df['temperature'] > 88) |
               (df['vibration'] > 0.6)).astype(int)
```

---

#### 슬라이드 48-50: 기본 모델 vs 최적화 모델

> "먼저 기본 설정으로 랜덤포레스트를 돌립니다."

```python
from sklearn.ensemble import RandomForestClassifier
from sklearn.model_selection import cross_val_score

model = RandomForestClassifier(random_state=42)
scores = cross_val_score(model, X_train, y_train, cv=5)
print(f"기본 모델: {scores.mean():.3f}")
```

> "이 점수를 GridSearchCV로 얼마나 올릴 수 있을까요?"

---

#### 슬라이드 51-53: GridSearchCV 적용

> "파라미터 그리드를 정의하고 탐색합니다."

```python
from sklearn.model_selection import GridSearchCV

param_grid = {
    'n_estimators': [50, 100, 200],
    'max_depth': [3, 5, 7, 10, None],
    'min_samples_split': [2, 5, 10]
}

grid_search = GridSearchCV(
    RandomForestClassifier(random_state=42),
    param_grid=param_grid,
    cv=5,
    scoring='f1',
    n_jobs=-1
)

grid_search.fit(X_train, y_train)
print(f"최적 파라미터: {grid_search.best_params_}")
print(f"최고 점수: {grid_search.best_score_:.3f}")
```

---

#### 슬라이드 54-56: RandomizedSearchCV 비교

> "같은 문제를 RandomizedSearchCV로 풀어봅니다."

```python
from sklearn.model_selection import RandomizedSearchCV
from scipy.stats import randint

param_dist = {
    'n_estimators': randint(50, 300),
    'max_depth': [3, 5, 7, 10, 15, None],
    'min_samples_split': randint(2, 20),
    'min_samples_leaf': randint(1, 10)
}

random_search = RandomizedSearchCV(
    model, param_dist, n_iter=50, cv=5, n_jobs=-1
)
random_search.fit(X_train, y_train)
```

> "탐색 시간과 최종 점수를 비교해봅니다. 대부분 Random이 더 빠르면서 비슷한 결과를 줍니다."

---

#### 슬라이드 57-59: 결과 비교 및 시각화

> "두 방법의 결과를 비교합니다."

```python
print(f"Grid - 조합: 45개, 시간: 15초, 점수: 0.87")
print(f"Random - 조합: 50개, 시간: 8초, 점수: 0.86")
```

> "Random이 절반 시간에 거의 같은 성능을 냈습니다. 실무에서는 Random으로 빠르게 탐색하고, 필요하면 Grid로 미세조정하는 전략을 씁니다."

---

### 정리 (2분)

#### 슬라이드 60-61: 핵심 정리

> "오늘 배운 내용을 정리합니다."

> "하이퍼파라미터는 사람이 설정하는 값이고, 성능에 큰 영향을 줍니다."

> "GridSearchCV는 모든 조합을 철저하게 탐색합니다. 시간이 오래 걸리지만 확실합니다."

> "RandomizedSearchCV는 랜덤 샘플링으로 빠르게 탐색합니다. 대부분 충분히 좋은 결과를 줍니다."

> "결과는 best_params_, best_score_, best_estimator_로 확인합니다."

---

#### 슬라이드 62-63: 다음 차시 예고

> "다음 시간에는 시계열 데이터를 다룹니다. 날짜와 시간이 있는 데이터의 특성을 이해하고, Python으로 처리하는 방법을 배웁니다."

> "오늘 수업 마무리합니다. 수고하셨습니다!"

---

## ❓ 예상 질문 및 답변

### Q1: Grid와 Random 중 어떤 걸 써야 하나요?

> "파라미터가 2~3개고 각각 몇 개 값만 테스트하면 Grid가 좋습니다. 파라미터가 많거나 범위가 넓으면 Random이 효율적이에요. 실무에서는 Random → Grid 2단계 전략을 많이 씁니다."

### Q2: n_iter는 몇으로 해야 하나요?

> "일반적으로 50~100 정도면 충분합니다. 중요한 모델이라면 200 이상으로 늘려도 됩니다. 시간과 성능의 트레이드오프를 고려하세요."

### Q3: n_jobs=-1이 뭔가요?

> "CPU의 모든 코어를 사용해서 병렬로 탐색합니다. 탐색 속도가 크게 빨라집니다. 다른 작업을 하면서 돌리려면 n_jobs=4처럼 일부만 쓰세요."

### Q4: scoring에 여러 지표를 동시에 쓸 수 있나요?

> "네, scoring=['accuracy', 'f1']처럼 리스트로 지정하고 refit='f1'로 최종 선택 기준을 정합니다. cv_results_에서 각 지표별 점수를 볼 수 있습니다."

### Q5: 최적 파라미터가 경계값이면 어떻게 하나요?

> "예를 들어 max_depth=[3,5,7]에서 7이 최적이면, 7보다 큰 값도 좋을 수 있습니다. 범위를 [7,10,15]로 확장해서 다시 탐색하세요."

---

## 📚 참고 자료

### 공식 문서
- [sklearn GridSearchCV](https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.GridSearchCV.html)
- [sklearn RandomizedSearchCV](https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.RandomizedSearchCV.html)

### 관련 차시
- 15차시: 모델 평가와 반복 검증
- 17차시: 시계열 데이터 기초

---

## ✅ 체크리스트

수업 전:
- [ ] GridSearchCV 예제 테스트
- [ ] RandomizedSearchCV 예제 테스트
- [ ] scipy.stats 설치 확인

수업 중:
- [ ] 파라미터 vs 하이퍼파라미터 구분
- [ ] param_grid 작성법 설명
- [ ] best_params_, best_score_ 결과 해석
- [ ] Grid vs Random 시간/성능 비교

수업 후:
- [ ] 실습 코드 배포
- [ ] 시계열 데이터 예고
