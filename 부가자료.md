# K-디지털 기초역량훈련 부가자료

## 부가자료 작성 안내
※ 학습퀴즈 1: OX 선택형
※ 학습퀴즈 2: 4지 선다형
※ 학습정리하기: 해당 차시 핵심 내용 요약

---

◎ 1차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
AI 윤리의 4대 원칙에는 공정성(Fairness), 투명성(Transparency), 책임성(Accountability), 안전성(Safety)이 포함된다.

<정답> O

<해설>
AI 윤리 4대 원칙은 공정성, 투명성, 책임성, 안전성임. 공정성은 편향 없이 동등하게 대우하는 것이고, 투명성은 AI 판단 과정을 설명할 수 있어야 함을 의미함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
개인정보를 보호하면서 데이터를 활용하기 위한 기술로 가장 적절한 것은?

<보기>
① 데이터 삭제
② 비식별화 처리
③ 데이터 공개
④ 접근 차단

<정답> 2번

<해설>
비식별화 처리는 개인을 식별할 수 없도록 데이터를 가공하는 기술임. 가명처리, 총계처리, 데이터 마스킹 등의 방법이 있으며, 데이터 활용과 개인정보 보호를 동시에 달성할 수 있음.

▶ 학습정리하기

- 이번 차시에서는 AI 활용 시 필요한 윤리적 고려사항과 데이터 보호 방법을 학습함.

- AI 윤리 4대 원칙(공정성, 투명성, 책임성, 안전성)의 의미와 중요성을 이해함. 특히 편향된 데이터로 학습된 AI가 불공정한 결과를 만들 수 있음을 인지함.

- 개인정보보호법의 기본 원칙과 비식별화 처리 기법을 파악함. 민감정보 마스킹, 데이터 접근 로그 관리 등 실무에서 적용 가능한 보안 조치를 익힘.

---

◎ 2차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
Python에서 리스트(list)는 서로 다른 자료형의 데이터를 함께 저장할 수 있다.

<정답> O

<해설>
Python 리스트는 다양한 자료형을 함께 저장할 수 있는 유연한 자료구조임. 예를 들어 [1, "hello", 3.14, True]와 같이 정수, 문자열, 실수, 불리언을 하나의 리스트에 담을 수 있음.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
Python에서 for 반복문과 함께 인덱스와 값을 동시에 얻고자 할 때 사용하는 함수는?

<보기>
① range()
② len()
③ enumerate()
④ zip()

<정답> 3번

<해설>
enumerate() 함수는 반복 가능한 객체를 순회할 때 인덱스와 값을 동시에 반환함. for i, value in enumerate(list)와 같이 사용하여 순서 번호와 요소 값을 함께 활용할 수 있음.

▶ 학습정리하기

- 이번 차시에서는 Python 프로그래밍의 기초를 학습함.

- Anaconda와 Jupyter Notebook 환경 설정 방법을 익힘. Jupyter Notebook은 코드 실행과 결과를 즉시 확인할 수 있어 데이터 분석에 적합함.

- Python의 5가지 기본 자료형(int, float, str, list, dict)과 제어문(if, for, while)을 학습함. f-string 포맷팅과 리스트 컴프리헨션 등 효율적인 코드 작성법을 익힘.

---

◎ 3차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
Pandas의 DataFrame에서 특정 조건을 만족하는 행만 선택할 때 df[df['column'] > 100]과 같은 불리언 인덱싱을 사용한다.

<정답> O

<해설>
Pandas에서 조건 필터링은 불리언 인덱싱을 사용함. df['column'] > 100은 True/False 시리즈를 반환하고, 이를 df[]에 전달하면 True인 행만 선택됨.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
Pandas DataFrame에서 그룹별 집계를 수행하는 메서드는?

<보기>
① merge()
② groupby()
③ concat()
④ pivot()

<정답> 2번

<해설>
groupby() 메서드는 특정 컬럼을 기준으로 데이터를 그룹화하고 집계 연산을 수행함. df.groupby('category').mean()과 같이 사용하여 그룹별 평균을 계산할 수 있음.

▶ 학습정리하기

- 이번 차시에서는 NumPy와 Pandas를 활용한 제조 데이터 처리를 학습함.

- NumPy 배열의 벡터화 연산으로 반복문 없이 빠른 계산이 가능함을 이해함. 배열 슬라이싱과 브로드캐스팅 개념을 익힘.

- Pandas DataFrame의 기본 조작법(loc, iloc, 조건 필터링)과 그룹별 집계(groupby, agg)를 실습함. CSV 파일 읽기/쓰기로 실제 데이터를 다루는 방법을 학습함.

---

◎ 4차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
공공데이터포털에서 제공하는 데이터는 누구나 무료로 활용할 수 있다.

<정답> O

<해설>
공공데이터포털(data.go.kr)은 정부와 공공기관의 데이터를 개방하는 플랫폼으로, 대부분의 데이터를 무료로 제공함. 다만 활용 시 출처 표기 등 이용 조건을 확인해야 함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
머신러닝 연구용 데이터셋을 가장 많이 제공하는 국제 플랫폼은?

<보기>
① 공공데이터포털
② Kaggle
③ 네이버 데이터랩
④ 국가통계포털

<정답> 2번

<해설>
Kaggle은 세계 최대의 데이터 사이언스 커뮤니티로, 수많은 데이터셋과 머신러닝 경진대회를 제공함. UCI ML Repository와 함께 연구용 데이터의 주요 출처임.

▶ 학습정리하기

- 이번 차시에서는 공개 데이터셋 확보 방법과 데이터 생태계를 이해함.

- 공공데이터포털, Kaggle, UCI ML Repository 등 주요 데이터 플랫폼의 특성과 활용법을 학습함. 각 플랫폼별 데이터 형식과 라이선스를 확인하는 방법을 익힘.

- KAMP(한국AI제조플랫폼) 등 제조 분야 특화 데이터셋의 존재를 파악함. 데이터 품질 확인과 메타데이터 해석의 중요성을 인지함.

---

◎ 5차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
박스플롯(Box Plot)에서 상자의 아래 경계는 1사분위수(Q1)를 나타낸다.

<정답> O

<해설>
박스플롯에서 상자의 아래 경계는 Q1(25%), 가운데 선은 중앙값(Q2, 50%), 위 경계는 Q3(75%)를 나타냄. 수염은 일반적으로 Q1-1.5×IQR ~ Q3+1.5×IQR 범위를 표시함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
데이터의 분포 형태를 연속적인 곡선으로 시각화하는 방법은?

<보기>
① 히스토그램
② KDE(커널 밀도 추정)
③ 산점도
④ 막대그래프

<정답> 2번

<해설>
KDE(Kernel Density Estimation)는 히스토그램과 달리 연속적인 곡선으로 데이터 분포를 시각화함. 데이터 포인트 주변에 커널 함수를 배치하고 합산하여 매끄러운 밀도 곡선을 생성함.

▶ 학습정리하기

- 이번 차시에서는 기초 기술통계량과 탐색적 시각화를 학습함.

- 평균, 중앙값, 표준편차 등 기술통계량의 의미와 계산 방법을 익힘. 평균은 이상치에 민감하고, 중앙값은 이상치에 강건함을 이해함.

- 히스토그램, 박스플롯, KDE 등 분포 시각화 방법을 실습함. 사분위수(Q1, Q2, Q3)와 IQR(사분위 범위)의 개념을 파악하고 데이터 분포 해석에 활용함.

---

◎ 6차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
정규분포에서 평균으로부터 ±2 표준편차 범위 내에 약 95%의 데이터가 포함된다.

<정답> O

<해설>
정규분포의 68-95-99.7 규칙에 따르면, 평균 ±1σ에 68%, ±2σ에 95%, ±3σ에 99.7%의 데이터가 포함됨. 이를 품질 관리에서 관리 한계선 설정에 활용함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
데이터가 정규분포를 따르는지 검정하는 방법은?

<보기>
① t-검정
② Shapiro-Wilk 검정
③ 카이제곱 검정
④ ANOVA

<정답> 2번

<해설>
Shapiro-Wilk 검정은 데이터의 정규성을 검정하는 대표적인 방법임. p-value가 0.05보다 크면 정규분포를 따른다고 볼 수 있음. 표본 크기가 작을 때 효과적임.

▶ 학습정리하기

- 이번 차시에서는 확률분포와 품질검정의 기초를 학습함.

- 정규분포의 특성과 68-95-99.7 규칙을 이해함. 표준정규분포로 변환하는 Z-score 개념을 익히고, 이상치 탐지에 활용하는 방법을 학습함.

- Shapiro-Wilk 검정으로 정규성을 확인하는 방법을 실습함. 제조 공정에서 품질 관리 한계선(UCL, LCL)을 설정하는 원리를 파악함.

---

◎ 7차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
가설검정에서 p-value가 0.05보다 작으면 귀무가설을 기각한다.

<정답> O

<해설>
유의수준(α) 0.05를 기준으로 p-value < 0.05이면 귀무가설을 기각하고 대립가설을 채택함. p-value는 귀무가설이 참일 때 현재 관측값 이상의 극단적인 결과가 나올 확률임.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
세 개 이상의 그룹 평균을 비교하는 통계 검정 방법은?

<보기>
① t-검정
② ANOVA (분산분석)
③ 피어슨 상관계수
④ 회귀분석

<정답> 2번

<해설>
ANOVA(Analysis of Variance)는 세 개 이상 그룹의 평균 차이를 검정함. 두 그룹 비교는 t-검정을, 세 그룹 이상은 ANOVA를 사용함. ANOVA 후 사후검정(Tukey HSD)으로 구체적 차이를 확인함.

▶ 학습정리하기

- 이번 차시에서는 가설검정의 개념과 다양한 통계검정 방법을 학습함.

- 귀무가설(H0)과 대립가설(H1)의 개념, p-value와 유의수준(α)의 관계를 이해함. 1종 오류(귀무가설이 참인데 기각)와 2종 오류(귀무가설이 거짓인데 채택)의 의미를 파악함.

- t-검정(두 그룹 평균 비교), ANOVA(세 그룹 이상), 카이제곱 검정(범주형 변수 독립성)의 적용 상황을 구분하고 실습함.

---

◎ 8차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
피어슨 상관계수가 -0.9이면 두 변수 간에 강한 음의 상관관계가 있다.

<정답> O

<해설>
피어슨 상관계수는 -1에서 1 사이의 값을 가짐. -0.9는 강한 음의 상관관계로, 한 변수가 증가하면 다른 변수가 감소하는 경향이 강함을 의미함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
단순선형회귀에서 모델이 데이터를 얼마나 잘 설명하는지 나타내는 지표는?

<보기>
① 표준편차
② 결정계수(R²)
③ 중앙값
④ 분산

<정답> 2번

<해설>
결정계수(R²)는 0에서 1 사이의 값으로, 회귀 모델이 종속변수의 변동을 얼마나 설명하는지 나타냄. R²=0.85이면 독립변수가 종속변수 변동의 85%를 설명함을 의미함.

▶ 학습정리하기

- 이번 차시에서는 상관분석과 회귀분석의 기초를 학습함.

- 피어슨 상관계수로 두 변수 간 선형 관계의 강도와 방향을 측정하는 방법을 익힘. 상관관계가 인과관계를 의미하지 않음을 이해함.

- 단순선형회귀의 최소제곱법 원리와 결정계수(R²) 해석을 학습함. 기울기와 절편의 의미, 잔차 분석의 중요성을 파악함.

---

◎ 9차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
결측치를 처리할 때 dropna()는 결측치가 있는 행 전체를 삭제한다.

<정답> O

<해설>
Pandas의 dropna() 메서드는 결측치(NaN)가 포함된 행을 삭제함. 데이터가 충분히 많고 결측 비율이 낮을 때 사용함. fillna()는 특정 값으로 결측치를 대체함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
IQR 방법으로 이상치를 탐지할 때 상한 기준선(Upper Fence)의 계산식은?

<보기>
① Q1 - 1.5 × IQR
② Q3 + 1.5 × IQR
③ Q2 + 2 × IQR
④ Q3 - 1.5 × IQR

<정답> 2번

<해설>
IQR 방법에서 상한 기준선은 Q3 + 1.5×IQR, 하한 기준선은 Q1 - 1.5×IQR임. IQR = Q3 - Q1이며, 이 범위를 벗어나는 데이터를 이상치로 판단함.

▶ 학습정리하기

- 이번 차시에서는 제조 데이터 전처리의 첫 번째 단계로 결측치와 이상치 처리를 학습함.

- 결측치의 유형(MCAR, MAR, MNAR)과 처리 방법(삭제, 대체)을 이해함. 상황에 따라 평균, 중앙값, 최빈값 또는 그룹별 통계량으로 대체하는 전략을 익힘.

- IQR 방법과 Z-score 방법으로 이상치를 탐지하는 방법을 실습함. 이상치를 삭제, 대체, 또는 플래그 추가하는 다양한 처리 전략을 파악함.

---

◎ 10차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
StandardScaler는 데이터를 평균 0, 표준편차 1로 변환하는 정규화 방법이다.

<정답> O

<해설>
StandardScaler는 z = (x - mean) / std 공식으로 데이터를 표준화함. 결과적으로 평균 0, 표준편차 1인 분포로 변환됨. 이는 특성 간 스케일 차이를 제거하여 모델 학습을 돕는 방법임.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
범주형 변수를 숫자로 변환할 때, 각 범주를 이진 벡터로 변환하는 방법은?

<보기>
① Label Encoding
② One-Hot Encoding
③ Standard Scaling
④ Min-Max Scaling

<정답> 2번

<해설>
One-Hot Encoding은 각 범주를 독립적인 이진 열로 변환함. 예: ['A', 'B', 'C'] → [[1,0,0], [0,1,0], [0,0,1]]. 범주 간 순서 관계가 없을 때 사용하며, Label Encoding은 순서가 있을 때 적합함.

▶ 학습정리하기

- 이번 차시에서는 스케일링과 인코딩을 포함한 고급 전처리 기법을 학습함.

- StandardScaler, MinMaxScaler, RobustScaler의 특성과 적용 상황을 비교함. 이상치가 많을 때는 RobustScaler가 효과적임을 이해함.

- 범주형 변수의 인코딩 방법(Label Encoding, One-Hot Encoding)을 실습함. sklearn의 Pipeline과 ColumnTransformer로 전처리 과정을 자동화하는 방법을 익힘.

---

◎ 11차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
VIF(분산팽창인자)가 10 이상이면 다중공선성 문제가 있다고 판단한다.

<정답> O

<해설>
VIF(Variance Inflation Factor)는 독립변수 간 상관관계로 인한 회귀계수 분산 증가를 측정함. VIF > 10이면 다중공선성이 심각하다고 판단하며, 해당 변수 제거나 PCA 적용을 고려함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
중다회귀분석에서 독립변수 개수가 증가해도 모델의 과적합을 보정하여 평가하는 지표는?

<보기>
① R²
② 조정 R² (Adjusted R²)
③ MSE
④ 표준편차

<정답> 2번

<해설>
조정 R²는 독립변수 개수를 고려하여 R²를 보정한 지표임. 불필요한 변수를 추가하면 R²는 증가하지만 조정 R²는 감소할 수 있어 모델 복잡도를 평가하는 데 유용함.

▶ 학습정리하기

- 이번 차시에서는 통계적 데이터 분석을 종합하고 한계점을 파악함.

- 중다회귀분석으로 여러 독립변수의 영향을 동시에 분석하는 방법을 학습함. 회귀계수 해석과 조정 R² 개념을 익힘.

- 다중공선성 문제를 VIF로 진단하고 해결하는 방법을 실습함. 통계 분석의 한계(선형성 가정, 비선형 패턴 미포착)를 인식하고 머신러닝 접근의 필요성을 이해함.

---

◎ 12차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
머신러닝에서 분류(Classification)는 이산적인 범주를 예측하고, 회귀(Regression)는 연속적인 수치를 예측한다.

<정답> O

<해설>
분류는 불량/정상, 스팸/정상 등 범주형 레이블을 예측하고, 회귀는 온도, 가격 등 연속적인 수치를 예측함. 문제 유형에 따라 적절한 모델과 평가 지표를 선택해야 함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
데이터를 학습용과 테스트용으로 분할하는 sklearn 함수는?

<보기>
① cross_val_score
② GridSearchCV
③ train_test_split
④ StandardScaler

<정답> 3번

<해설>
train_test_split은 데이터를 학습용과 테스트용으로 분할하는 함수임. 일반적으로 80:20 또는 70:30 비율로 분할하며, random_state 파라미터로 재현 가능한 분할을 보장함.

▶ 학습정리하기

- 이번 차시에서는 머신러닝의 기본 개념과 문제 유형을 학습함.

- 머신러닝의 정의(데이터에서 패턴을 학습하여 예측)와 전통적 프로그래밍과의 차이를 이해함. 특성(Feature)과 타겟(Target)의 개념을 파악함.

- 지도학습(분류, 회귀)과 비지도학습(군집화, 차원축소)의 차이를 구분함. train_test_split으로 데이터를 분할하고 fit-predict 패턴으로 모델을 학습하고 예측하는 기본 흐름을 익힘.

---

◎ 13차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
로지스틱 회귀는 이름에 '회귀'가 포함되어 있지만 실제로는 분류 문제에 사용된다.

<정답> O

<해설>
로지스틱 회귀는 시그모이드 함수를 사용하여 0과 1 사이의 확률값을 출력하고, 임계값(보통 0.5)을 기준으로 클래스를 분류함. 따라서 이진 분류 문제에 널리 사용됨.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
다항회귀에서 차수를 너무 높이면 발생할 수 있는 문제는?

<보기>
① 과소적합 (Underfitting)
② 과대적합 (Overfitting)
③ 결측치 발생
④ 정규성 위반

<정답> 2번

<해설>
다항회귀에서 차수를 높이면 모델이 학습 데이터에 지나치게 맞춰져 과대적합이 발생할 수 있음. 이 경우 학습 데이터에서는 성능이 좋지만 새로운 데이터에서는 성능이 떨어짐.

▶ 학습정리하기

- 이번 차시에서는 회귀모델의 확장인 다항회귀와 로지스틱회귀를 학습함.

- 다항회귀로 비선형 관계를 모델링하는 방법을 익힘. PolynomialFeatures로 특성을 확장하고, 차수 선택 시 과적합을 고려해야 함을 이해함.

- 로지스틱회귀의 시그모이드 함수와 확률 출력 원리를 파악함. 오즈비와 로그 오즈(logit)의 개념을 학습하고, 이진 분류에 적용하는 방법을 실습함.

---

◎ 14차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
의사결정나무에서 지니 불순도(Gini Impurity)가 0이면 해당 노드는 순수하게 하나의 클래스만 포함한다.

<정답> O

<해설>
지니 불순도는 노드의 불순도를 측정하며, 0~0.5 사이의 값을 가짐(이진 분류 기준). 0이면 모든 샘플이 같은 클래스에 속하는 순수한 노드임을 의미함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
의사결정나무의 과대적합을 방지하기 위해 조절하는 하이퍼파라미터는?

<보기>
① n_estimators
② learning_rate
③ max_depth
④ n_neighbors

<정답> 3번

<해설>
max_depth는 트리의 최대 깊이를 제한하여 과대적합을 방지함. 깊이가 너무 깊으면 학습 데이터에 과적합되고, 너무 얕으면 과소적합이 발생함. 적절한 값을 찾는 것이 중요함.

▶ 학습정리하기

- 이번 차시에서는 의사결정나무의 원리와 구현을 학습함.

- 의사결정나무가 데이터를 분할하는 기준인 지니 불순도와 정보 이득의 개념을 이해함. 루트 노드에서 리프 노드까지 분할이 진행되는 과정을 파악함.

- max_depth, min_samples_split 등 하이퍼파라미터로 트리 복잡도를 조절하는 방법을 실습함. 특성 중요도(feature_importances_)로 변수의 영향력을 해석하는 방법을 익힘.

---

◎ 15차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
랜덤포레스트는 여러 개의 의사결정나무를 학습시키고 다수결 투표로 최종 예측을 수행하는 앙상블 방법이다.

<정답> O

<해설>
랜덤포레스트는 배깅(Bagging) 기반 앙상블 방법으로, 부트스트랩 샘플링으로 여러 트리를 독립적으로 학습함. 분류 문제에서는 다수결, 회귀 문제에서는 평균으로 최종 예측을 수행함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
랜덤포레스트에서 트리의 개수를 결정하는 하이퍼파라미터는?

<보기>
① max_depth
② n_estimators
③ min_samples_split
④ max_features

<정답> 2번

<해설>
n_estimators는 랜덤포레스트에서 생성할 트리의 개수를 지정함. 트리 수가 많을수록 성능이 안정되지만 학습 시간이 증가함. 일반적으로 100~500 사이의 값을 사용함.

▶ 학습정리하기

- 이번 차시에서는 앙상블 학습과 랜덤포레스트를 학습함.

- 앙상블 학습의 개념(여러 모델 결합)과 배깅(Bagging) vs 부스팅(Boosting)의 차이를 이해함. 집단 지성 원리로 개별 모델의 오류가 상쇄됨을 파악함.

- 랜덤포레스트의 두 가지 랜덤성(데이터 랜덤 샘플링, 특성 랜덤 선택)을 학습함. n_estimators, max_depth 등 주요 하이퍼파라미터와 OOB(Out-Of-Bag) 점수의 의미를 익힘.

---

◎ 16차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
비지도학습은 정답 레이블 없이 데이터의 패턴이나 구조를 발견하는 학습 방법이다.

<정답> O

<해설>
비지도학습은 레이블이 없는 데이터에서 군집, 이상치, 잠재 구조 등을 발견함. 클러스터링, 차원축소, 이상탐지 등이 대표적이며, 탐색적 분석이나 레이블 획득 비용이 높을 때 유용함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
K-Means 클러스터링에서 최적의 군집 수(K)를 결정하는 방법은?

<보기>
① 교차검증
② 엘보우 방법 (Elbow Method)
③ 그리드 서치
④ 부트스트랩

<정답> 2번

<해설>
엘보우 방법은 K 값에 따른 군집 내 분산(Inertia)을 그래프로 그려 꺾이는 지점(팔꿈치)을 최적 K로 선택하는 방법임. 실루엣 점수(Silhouette Score)도 함께 활용할 수 있음.

▶ 학습정리하기

- 이번 차시에서는 비지도학습의 개념과 K-Means 클러스터링을 학습함.

- 지도학습과 비지도학습의 차이, 비지도학습이 필요한 상황(레이블 없음, 탐색적 분석)을 이해함. 군집화, 차원축소, 이상탐지 등 비지도학습의 유형을 파악함.

- K-Means 알고리즘의 원리(중심점 할당-갱신 반복)와 엘보우 방법으로 최적 K를 선택하는 방법을 실습함. 거리 기반 이상 탐지 개념을 익힘.

---

◎ 17차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
K-Fold 교차검증에서 K=5이면 데이터를 5등분하여 각 부분이 한 번씩 테스트 세트로 사용된다.

<정답> O

<해설>
K-Fold 교차검증은 데이터를 K개로 분할하고, 각 폴드가 한 번씩 테스트 세트로 사용되어 총 K번 학습-평가를 수행함. 최종 성능은 K번의 평균으로 계산되어 신뢰성이 높아짐.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
학습 데이터에서는 성능이 높지만 테스트 데이터에서 성능이 낮은 현상은?

<보기>
① 과소적합 (Underfitting)
② 과대적합 (Overfitting)
③ 정규화
④ 교차검증

<정답> 2번

<해설>
과대적합은 모델이 학습 데이터에 지나치게 맞춰져 새로운 데이터에 일반화하지 못하는 현상임. 학습 정확도는 높지만 테스트 정확도가 낮은 것이 특징이며, 모델 복잡도 조절로 해결함.

▶ 학습정리하기

- 이번 차시에서는 교차검증과 모델 적합도 진단을 학습함.

- 단순 학습/테스트 분할의 한계와 K-Fold 교차검증의 장점(안정적 평가, 전체 데이터 활용)을 이해함. cross_val_score 사용법을 익힘.

- 과대적합과 과소적합의 개념, 학습 곡선으로 진단하는 방법을 학습함. 정밀도, 재현율, F1-score, 혼동행렬 등 분류 평가 지표의 의미와 활용법을 파악함.

---

◎ 18차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
하이퍼파라미터는 모델이 학습 과정에서 자동으로 찾는 값이 아니라 사람이 미리 설정해야 하는 값이다.

<정답> O

<해설>
파라미터(가중치, 절편)는 학습 과정에서 자동으로 결정되지만, 하이퍼파라미터(max_depth, n_estimators 등)는 학습 전에 사람이 설정해야 함. 하이퍼파라미터 최적화로 모델 성능을 향상시킬 수 있음.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
모든 하이퍼파라미터 조합을 격자 형태로 탐색하는 방법은?

<보기>
① RandomizedSearchCV
② GridSearchCV
③ cross_val_score
④ train_test_split

<정답> 2번

<해설>
GridSearchCV는 지정한 하이퍼파라미터의 모든 조합을 탐색하고 교차검증으로 평가함. 철저하지만 조합 수가 많으면 시간이 오래 걸림. RandomizedSearchCV는 랜덤 샘플링으로 효율적 탐색을 수행함.

▶ 학습정리하기

- 이번 차시에서는 하이퍼파라미터 최적화 방법을 학습함.

- 파라미터와 하이퍼파라미터의 차이, 하이퍼파라미터가 모델 성능에 미치는 영향을 이해함. 주요 모델별 핵심 하이퍼파라미터를 파악함.

- GridSearchCV(전수 탐색)와 RandomizedSearchCV(랜덤 탐색)의 원리와 장단점을 비교함. best_params_, best_score_로 최적 조합을 확인하는 방법을 실습함.

---

◎ 19차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
이동평균(Moving Average)은 시계열 데이터의 노이즈를 줄이고 추세를 파악하는 데 사용된다.

<정답> O

<해설>
이동평균은 일정 윈도우 크기 내의 데이터를 평균하여 단기 변동(노이즈)을 완화하고 장기 추세를 파악함. 윈도우 크기가 클수록 더 매끄러운 곡선이 되지만 민감도는 감소함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
시계열 예측에서 과거 시점의 값을 특성으로 사용하는 기법은?

<보기>
① One-Hot Encoding
② Lag 특성 (Lag Feature)
③ 표준화
④ PCA

<정답> 2번

<해설>
Lag 특성은 과거 시점의 값(t-1, t-2 등)을 현재 시점의 특성으로 사용하는 기법임. 예를 들어 어제 온도로 오늘 온도를 예측할 때 lag=1 특성을 생성함. 시계열의 자기상관성을 활용함.

▶ 학습정리하기

- 이번 차시에서는 시계열 데이터의 특성과 분석 방법을 학습함.

- 시계열 데이터의 구성 요소(추세, 계절성, 노이즈)와 datetime 처리 방법을 이해함. Pandas의 시계열 인덱싱과 리샘플링을 익힘.

- Lag 특성, 이동평균 특성, 날짜 특성(요일, 월) 등 시계열 특성 엔지니어링을 실습함. RandomForest로 시계열 예측을 수행하고 MAE, RMSE, MAPE로 평가하는 방법을 학습함.

---

◎ 20차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
딥러닝은 여러 층(Layer)을 쌓은 인공 신경망을 사용하여 복잡한 패턴을 학습하는 방법이다.

<정답> O

<해설>
딥러닝(Deep Learning)은 2개 이상의 은닉층을 가진 신경망을 사용함. 층이 깊어질수록 더 복잡하고 추상적인 패턴을 학습할 수 있으며, 이미지, 텍스트, 음성 등 다양한 데이터에 적용됨.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
인공 뉴런에서 가중합에 비선형성을 추가하는 함수는?

<보기>
① 손실 함수
② 활성화 함수
③ 비용 함수
④ 정규화 함수

<정답> 2번

<해설>
활성화 함수는 가중합(z = wx + b)에 비선형성을 추가함. ReLU, Sigmoid, Tanh 등이 있으며, 비선형 활성화가 없으면 신경망은 선형 모델과 동일해짐.

▶ 학습정리하기

- 이번 차시에서는 딥러닝의 기초 개념인 인공 신경망을 학습함.

- 생물학적 뉴런과 인공 뉴런(퍼셉트론)의 대응 관계를 이해함. 가중치, 편향, 활성화 함수의 역할을 파악함.

- 신경망의 구조(입력층, 은닉층, 출력층)와 순전파(입력→출력 계산), 역전파(오차→기울기 계산) 개념을 학습함. 머신러닝과 딥러닝의 차이점을 비교함.

---

◎ 21차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
단일 퍼셉트론은 XOR 문제를 해결할 수 없지만, 다층 퍼셉트론(MLP)은 해결할 수 있다.

<정답> O

<해설>
XOR은 선형 분리가 불가능한 문제로, 단일 퍼셉트론으로는 해결할 수 없음. 은닉층을 추가한 다층 퍼셉트론(MLP)은 비선형 결정 경계를 만들어 XOR 문제를 해결할 수 있음.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
신경망에서 역전파(Backpropagation)의 주된 목적은?

<보기>
① 입력 데이터 전처리
② 예측값 계산
③ 가중치 업데이트를 위한 기울기 계산
④ 데이터 증강

<정답> 3번

<해설>
역전파는 출력층의 오차를 역방향으로 전파하며 각 가중치에 대한 손실 함수의 기울기를 계산함. 이 기울기를 사용하여 경사하강법으로 가중치를 업데이트하고 모델을 학습시킴.

▶ 학습정리하기

- 이번 차시에서는 퍼셉트론의 한계와 다층 퍼셉트론(MLP)의 등장을 학습함.

- 퍼셉트론으로 AND, OR, NOT 논리 게이트를 구현하는 방법을 이해함. XOR 문제가 선형 분리 불가능하여 단일 퍼셉트론의 한계를 드러냄을 파악함.

- 은닉층 추가로 비선형 패턴을 학습하는 MLP의 원리를 학습함. 순전파(정보 흐름)와 역전파(기울기 계산)의 과정을 직관적으로 이해함.

---

◎ 22차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
Keras의 Sequential 모델은 층(Layer)을 순서대로 쌓아 신경망을 구성하는 방법이다.

<정답> O

<해설>
Keras Sequential 모델은 층을 선형으로 쌓는 간단한 방법임. model.add(Dense(64, activation='relu'))와 같이 층을 순차적으로 추가하여 신경망을 구성함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
Keras에서 모델 학습에 사용하는 메서드는?

<보기>
① model.predict()
② model.compile()
③ model.fit()
④ model.evaluate()

<정답> 3번

<해설>
model.fit(X, y, epochs=10)은 모델을 학습시키는 메서드임. compile()은 손실함수와 옵티마이저를 설정하고, predict()는 예측, evaluate()는 평가를 수행함.

▶ 학습정리하기

- 이번 차시에서는 Keras를 활용한 MLP 품질 예측 실습을 수행함.

- Keras 프레임워크의 특징(직관적 API, TensorFlow 기반)과 Sequential 모델 생성 방법을 익힘. Dense 층, 활성화 함수(ReLU, Sigmoid, Softmax)의 사용법을 학습함.

- model.compile(), model.fit(), model.predict() 패턴으로 모델을 구성, 학습, 예측하는 과정을 실습함. 학습 곡선으로 과적합을 모니터링하는 방법을 파악함.

---

◎ 23차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
오토인코더는 입력을 압축(인코더)했다가 다시 복원(디코더)하는 구조로, 재구성 오차가 큰 데이터를 이상치로 탐지할 수 있다.

<정답> O

<해설>
오토인코더는 정상 데이터로 학습하여 정상 패턴을 압축-복원함. 이상 데이터는 학습되지 않은 패턴이므로 재구성 오차가 크게 발생하며, 이를 기준으로 이상치를 탐지함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
오토인코더의 중간에 위치하며 입력 데이터의 압축된 표현을 담는 부분은?

<보기>
① 입력층
② 출력층
③ 잠재 공간 (Latent Space)
④ 활성화 함수

<정답> 3번

<해설>
잠재 공간(Latent Space)은 인코더 출력이자 디코더 입력으로, 입력 데이터의 핵심 정보를 압축한 저차원 표현임. 잠재 공간의 차원이 작을수록 더 강한 압축이 이루어짐.

▶ 학습정리하기

- 이번 차시에서는 오토인코더 기반 이상 탐지를 학습함.

- 오토인코더의 인코더-잠재공간-디코더 구조와 재구성 학습 원리를 이해함. 입력 차원보다 작은 잠재 공간으로 데이터의 핵심 특성을 학습함을 파악함.

- 정상 데이터만으로 오토인코더를 학습하고, 재구성 오차(MSE) 기반으로 이상치를 탐지하는 방법을 실습함. 임계값 설정과 이상 탐지 성능 평가 방법을 익힘.

---

◎ 24차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
CNN(합성곱 신경망)의 합성곱 층은 이미지에서 국소적인 패턴(엣지, 텍스처 등)을 추출한다.

<정답> O

<해설>
합성곱 층은 커널(필터)을 이미지 위에 슬라이딩하며 국소적 특징을 추출함. 얕은 층은 엣지, 색상 등 저수준 특징을, 깊은 층은 형태, 객체 등 고수준 특징을 학습함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
CNN에서 특성 맵의 크기를 줄이고 위치 불변성을 제공하는 층은?

<보기>
① 합성곱 층 (Conv2D)
② 풀링 층 (MaxPooling)
③ Dense 층
④ Flatten 층

<정답> 2번

<해설>
풀링 층(MaxPooling, AveragePooling)은 특성 맵을 다운샘플링하여 크기를 줄이고 계산량을 감소시킴. 또한 작은 위치 변화에 강건한 특징을 추출하여 위치 불변성을 제공함.

▶ 학습정리하기

- 이번 차시에서는 CNN 기반 이미지 불량 검출을 학습함.

- CNN의 구조(합성곱 층, 풀링 층, 완전연결 층)와 각 층의 역할을 이해함. 커널/필터의 슬라이딩 연산과 특성 맵 생성 원리를 파악함.

- Keras로 CNN 모델을 구성하고 주조 제품 이미지 불량 분류를 실습함. 데이터 증강, Dropout 등 과적합 방지 기법을 익힘.

---

◎ 25차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
LSTM은 RNN의 장기 의존성 문제를 해결하기 위해 게이트 구조를 도입한 모델이다.

<정답> O

<해설>
RNN은 시퀀스가 길어지면 기울기 소실로 인해 먼 과거 정보를 기억하기 어려움. LSTM은 Forget Gate, Input Gate, Output Gate를 사용하여 정보의 기억과 망각을 조절하고 장기 의존성 문제를 해결함.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
RNN에서 이전 시점의 정보를 현재 시점으로 전달하는 상태는?

<보기>
① 입력 상태
② 은닉 상태 (Hidden State)
③ 출력 상태
④ 손실 상태

<정답> 2번

<해설>
은닉 상태(Hidden State)는 이전 시점의 정보를 요약하여 다음 시점으로 전달하는 RNN의 핵심 메커니즘임. 순환 연결을 통해 시퀀스 정보를 유지하고 시계열 패턴을 학습함.

▶ 학습정리하기

- 이번 차시에서는 RNN과 LSTM을 활용한 시계열 예측을 학습함.

- RNN의 순환 구조와 은닉 상태를 통한 시퀀스 정보 전달 원리를 이해함. 기울기 소실 문제로 인한 장기 의존성 학습의 한계를 파악함.

- LSTM의 게이트(Forget, Input, Output) 구조와 셀 상태(Cell State)의 역할을 학습함. Keras로 LSTM 모델을 구성하여 센서 시계열 예측을 실습함.

---

◎ 26차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
Grad-CAM은 CNN 모델이 이미지의 어느 부분을 보고 판단했는지 시각화하는 기법이다.

<정답> O

<해설>
Grad-CAM(Gradient-weighted Class Activation Mapping)은 특정 클래스에 대한 기울기를 활용하여 모델이 주목한 이미지 영역을 히트맵으로 시각화함. 딥러닝 모델의 해석 가능성(XAI)을 높이는 대표적 기법임.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
사전학습된 모델의 일부 층을 고정하고 나머지 층만 학습시키는 기법은?

<보기>
① 데이터 증강
② 파인튜닝 (Fine-tuning)
③ 정규화
④ 배치 정규화

<정답> 2번

<해설>
파인튜닝은 사전학습된 모델(예: ImageNet 학습 모델)의 얕은 층은 고정하고 깊은 층만 새 데이터로 재학습하는 기법임. 적은 데이터로도 높은 성능을 얻을 수 있어 전이학습의 핵심 기법임.

▶ 학습정리하기

- 이번 차시에서는 모델 해석과 파인튜닝 실무 전략을 학습함.

- Grad-CAM으로 CNN 모델의 판단 근거를 시각화하는 방법을 익힘. XAI(설명 가능한 AI)의 필요성과 블랙박스 문제를 이해함.

- TensorBoard와 Callbacks(EarlyStopping, ModelCheckpoint)로 학습 과정을 추적하고 관리하는 방법을 학습함. 전이학습과 파인튜닝으로 사전학습 모델을 활용하는 전략을 파악함.

---

◎ 27차시

▶ 학습퀴즈 1
※ OX 선택형 문항

<문제>
REST API에서 POST 메서드는 서버에 새로운 데이터를 생성하거나 ML 모델에 예측을 요청할 때 사용한다.

<정답> O

<해설>
POST 메서드는 HTTP에서 데이터를 서버로 전송하여 새 자원을 생성하거나, ML 서비스에서는 센서 데이터를 전송하고 예측 결과를 받을 때 사용함. GET 메서드는 데이터 조회용으로 사용됨.

▶ 학습퀴즈 2
※ 4지 선다형 문항

<문제>
Streamlit 웹앱을 실행하는 명령어로 올바른 것은?

<보기>
① python app.py
② streamlit run app.py
③ uvicorn app:app
④ flask run app.py

<정답> 2번

<해설>
Streamlit 앱은 `streamlit run 파일명.py` 명령어로 실행함. 기본 포트는 8501이며, 브라우저에서 http://localhost:8501로 접속함. uvicorn은 FastAPI 서버 실행 명령어임.

▶ 학습정리하기

- 이번 차시에서는 REST API 기초부터 ML 모델 서비스 배포까지 학습함.

- HTTP 메서드(GET, POST)와 상태 코드(200 성공, 400 잘못된 요청, 500 서버 오류)의 의미를 이해함. requests 라이브러리를 활용한 API 호출을 실습하여 외부 서비스와 통신하는 방법을 익힘.

- Streamlit으로 사용자가 직접 조작하는 웹앱을 구축함. st.title(), st.slider(), st.button() 등 직관적인 함수로 인터랙티브한 대시보드를 만들 수 있음.

- FastAPI로 외부 시스템이 호출하는 API 엔드포인트를 구현함. @app.post() 데코레이터로 엔드포인트를 정의하고, Pydantic 모델로 입력 데이터를 검증함.

- Docker 컨테이너 배포 기초도 다룸. Dockerfile 작성과 이미지 빌드, 컨테이너 실행의 흐름을 이해함. 컨테이너를 활용하면 어느 환경에서든 동일하게 동작하는 배포가 가능함.

- 27차시에 걸친 전체 과정을 통해 데이터 수집부터 전처리, 탐색적 분석, 머신러닝 모델 학습, 딥러닝, 모델 해석, 그리고 서비스 배포까지 데이터 분석의 전 과정을 완성함.
