# [11차시] 분류 모델 (1): 의사결정나무 - 다이어그램

## 1. 의사결정나무 개념

```mermaid
flowchart TD
    A["온도 > 88°C?"] --> |예| B["습도 > 60%?"]
    A --> |아니오| C["정상"]

    B --> |예| D["불량"]
    B --> |아니오| E["속도 < 90?"]

    E --> |예| F["불량"]
    E --> |아니오| G["정상"]

    style D fill:#ffcccc
    style F fill:#ffcccc
    style C fill:#ccffcc
    style G fill:#ccffcc
```

## 2. 트리 구조 용어

```mermaid
flowchart TD
    subgraph 구조["트리 구조"]
        A["Root Node<br>(첫 질문)"] --> B["Branch Node<br>(중간 질문)"]
        A --> C["Leaf Node<br>(최종 결과)"]
        B --> D["Leaf Node"]
        B --> E["Leaf Node"]
    end

    F["Root → Branch → Leaf"]
```

## 3. 좋은 질문 vs 나쁜 질문

```mermaid
flowchart LR
    subgraph 좋은["✅ 좋은 질문"]
        A1["온도 > 88?"]
        A1 --> B1["불량 8개"]
        A1 --> C1["정상 42개"]
        D1["순도 높음!"]
    end

    subgraph 나쁜["❌ 나쁜 질문"]
        A2["습도 > 50?"]
        A2 --> B2["불량 3, 정상 22"]
        A2 --> C2["불량 5, 정상 20"]
        D2["섞여 있음"]
    end
```

## 4. 정보 이득

```mermaid
flowchart TD
    A[원본 데이터] --> B["질문: 온도 > 88?"]

    B --> C["왼쪽: 불량 많음<br>(순도 높음)"]
    B --> D["오른쪽: 정상 많음<br>(순도 높음)"]

    E["정보 이득 = 순도 증가량"]

    F["높은 정보 이득 → 좋은 질문"]
```

## 5. sklearn 사용 흐름

```mermaid
flowchart TD
    A["1. 모델 생성<br>model = DecisionTreeClassifier()"]
    B["2. 학습<br>model.fit(X_train, y_train)"]
    C["3. 예측<br>model.predict(X_test)"]
    D["4. 확률 예측<br>model.predict_proba(X_test)"]
    E["5. 평가<br>model.score(X_test, y_test)"]

    A --> B --> C --> D --> E
```

## 6. predict vs predict_proba

```mermaid
flowchart LR
    subgraph 입력["입력 데이터"]
        A["온도: 90°C<br>습도: 55%<br>속도: 100"]
    end

    B[["모델"]]

    subgraph predict["predict()"]
        C["불량 (1)"]
    end

    subgraph proba["predict_proba()"]
        D["정상: 25%<br>불량: 75%"]
    end

    입력 --> B
    B --> predict
    B --> proba
```

## 7. 과대적합 문제

```mermaid
flowchart LR
    subgraph 과대적합["❌ 과대적합"]
        A1["깊은 트리<br>(depth=무제한)"]
        B1["학습: 99%<br>테스트: 70%"]
        C1["외움!"]
    end

    subgraph 적절["✅ 적절한 적합"]
        A2["얕은 트리<br>(depth=5)"]
        B2["학습: 85%<br>테스트: 83%"]
        C2["이해!"]
    end
```

## 8. max_depth 효과

```mermaid
flowchart TD
    A["max_depth 설정"]

    A --> B["depth=1"]
    A --> C["depth=3"]
    A --> D["depth=10"]
    A --> E["depth=None"]

    B --> B1["너무 단순<br>과소적합"]
    C --> C1["적절한 복잡도"]
    D --> D1["복잡함"]
    E --> E1["과대적합 위험"]

    style C1 fill:#c8e6c9
```

## 9. 학습/테스트 정확도 비교

```mermaid
flowchart TD
    A["정확도 비교"] --> B{"차이가 10%p 이상?"}

    B --> |예| C["⚠️ 과대적합<br>max_depth 줄이기"]
    B --> |아니오| D["✅ 적절한 일반화"]

    E["학습: 95%<br>테스트: 70%"] --> B
    F["학습: 85%<br>테스트: 83%"] --> B
```

## 10. 특성 중요도

```mermaid
pie title 특성 중요도 (예시)
    "온도" : 55
    "습도" : 30
    "속도" : 15
```

## 11. 의사결정나무 장단점

```mermaid
mindmap
  root((의사결정나무))
    장점
      해석 쉬움
      시각화 가능
      전처리 간단
      빠른 학습
    단점
      과대적합 위험
      불안정함
      성능 한계
```

## 12. 전체 모델링 흐름

```mermaid
flowchart TD
    A[데이터 준비] --> B[X, y 분리]
    B --> C[학습/테스트 분리]
    C --> D[모델 생성]
    D --> E["fit(학습)"]
    E --> F["predict(예측)"]
    F --> G["score(평가)"]
    G --> H{성능 만족?}
    H --> |아니오| I[하이퍼파라미터 조정]
    I --> D
    H --> |예| J[모델 완성]
```

## 13. 강의 구조

```mermaid
gantt
    title 11차시 강의 구조 (25-30분)
    dateFormat  mm:ss
    axisFormat %M:%S

    section 이론편
    도입 (인사/복습)           :a1, 00:00, 2m
    의사결정나무란?           :a2, after a1, 2m
    트리 구성 요소            :a3, after a2, 1m
    정보 이득                :a4, after a3, 1.5m
    sklearn 사용법           :a5, after a4, 2m
    과대적합 주의            :a6, after a5, 1.5m

    section 실습편
    실습 소개               :b1, after a6, 2m
    데이터 생성              :b2, after b1, 2m
    데이터 분리              :b3, after b2, 2m
    모델 학습               :b4, after b3, 2m
    예측                   :b5, after b4, 2m
    성능 평가               :b6, after b5, 2m
    트리 시각화              :b7, after b6, 2m
    특성 중요도              :b8, after b7, 2m

    section 정리
    핵심 요약               :c1, after b8, 1.5m
    다음 차시 예고           :c2, after c1, 1.5m
```

## 14. 의사결정나무 → 랜덤포레스트

```mermaid
flowchart LR
    subgraph 단일["의사결정나무 (11차시)"]
        A["🌲 하나의 트리"]
        B["불안정<br>과대적합 위험"]
    end

    subgraph 앙상블["랜덤포레스트 (12차시)"]
        C["🌲🌲🌲 여러 트리"]
        D["투표로 결정<br>안정적"]
    end

    단일 --> |개선| 앙상블
```
