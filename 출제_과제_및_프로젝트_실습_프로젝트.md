# 실습 프로젝트

**관련 차시: 27차시 (마지막 차시) - ML 모델 서비스 배포**

## 프로젝트 목표

전체 1~27차시를 종합하여 "문제정의 → 데이터 분석 → 모델링 → 평가 → 배포"
완전한 ML 파이프라인 구축

---

## 데이터 출처 및 다운로드 안내

본 프로젝트에서 사용하는 데이터는 **KAMP(한국AI제조플랫폼) 제조AI데이터셋**의 실제 변수명과 값 범위를 기반으로 생성된 합성 데이터입니다.

### 실제 데이터 다운로드 방법

**[공공데이터포털 KAMP 제조AI데이터셋]**
- **URL**: https://www.data.go.kr/data/15089213/fileData.do
- **데이터명**: KAMP 한국형 AI 제조 데이터셋
- **제공 기관**: 한국산업지능화협회

**다운로드 절차:**
1. 위 URL에 접속하여 공공데이터포털 로그인
2. "데이터 다운로드" 버튼 클릭
3. ZIP 파일 다운로드 후 압축 해제
4. 원하는 공정 데이터(사출성형, CNC, 프레스, 용접 등) 선택하여 사용

**참고 자료:**
- **KAMP 플랫폼**: https://www.kamp-ai.kr/
- **제조AI 가이드북**: 각 공정별 변수 설명 및 활용 가이드 제공

> **안내**: 본 과제에서는 학습 편의를 위해 KAMP 스펙 기반의 합성 데이터(`data/project_welding.csv`)를 제공합니다. 실제 프로젝트나 심화 학습 시에는 위 공공데이터포털에서 실제 데이터를 다운로드하여 활용해 보세요.

---

## <초급난이도> 제조 품질 예측 서비스 구축

**프로젝트 개요:**
당신은 A제조사의 데이터 분석팀 신입사원입니다. 용접 공정의 불량률을 줄이기 위한 **품질 예측 시스템** 구축 프로젝트에 투입되었습니다. 각 단계별로 주어진 시나리오를 읽고, 실제 업무 상황처럼 **코드를 수정**하고 **분석 결과를 도출**하세요.

**제출내용:**
각 Step에서 제시된 시나리오를 해결하기 위해 코드를 실행/수정하고, 요구되는 산출물(수정된 코드, 실행 결과, 분석 보고서 등)을 제출하세요.

---

### Step 1: 문제 정의 (10점)

```python
# ========================================
# Step 1: 문제 정의
# ========================================

print("=" * 50)
print("KAMP 용접 품질 예측 프로젝트")
print("=" * 50)

print("""
[비즈니스 문제]
용접 공정에서 불량 제품이 발생하면 재작업 비용과 납기 지연이 발생합니다.
센서 데이터를 기반으로 불량을 사전에 예측하여 품질 관리를 개선하고자 합니다.

[입력 변수 (X)]
- current: 용접전류 (A)
- voltage: 전압 (V)
- wire_feed_speed: 와이어송급속도 (m/min)
- travel_speed: 용접속도 (cm/min)
- gas_flow: 가스유량 (L/min)
- arc_length: 아크길이 (mm)
- heat_input: 입열량 (kJ/cm)

[출력 변수 (Y)]
- quality: 품질 (0=정상, 1=불량)

[성공 기준]
- 테스트 정확도 90% 이상 달성
""")
```

**[시나리오 1-1]**
> 생산관리팀장이 "용접 불량 예측 시스템"의 도입을 검토하고 있습니다. 팀장에게 보고할 **시스템 개요서**를 작성해야 합니다.

**과제:**
1. 위 코드를 실행하여 프로젝트 내용을 파악하세요
2. 아래 양식에 맞춰 **시스템 개요서**를 작성하세요

**[시스템 개요서 양식]**

| 항목 | 내용 |
|------|------|
| **시스템 목적** | (어떤 문제를 해결하는가?) |
| **필요한 센서 데이터** | 1. current - (역할 설명)<br>2. voltage - (역할 설명)<br>3. wire_feed_speed - (역할 설명)<br>4. travel_speed - (역할 설명)<br>5. gas_flow - (역할 설명)<br>6. arc_length - (역할 설명)<br>7. heat_input - (역할 설명) |
| **예측 결과** | (출력 변수와 해석 방법) |
| **기대 효과** | (정량적 목표 포함) |

---

### Step 2: 탐색적 데이터 분석 (15점)

```python
# ========================================
# Step 2: 탐색적 데이터 분석 (EDA)
# ========================================

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

# 한글 폰트 설정
import platform
if platform.system() == 'Windows':
    plt.rcParams['font.family'] = 'Malgun Gothic'
elif platform.system() == 'Darwin':
    plt.rcParams['font.family'] = 'AppleGothic'
else:
    plt.rcParams['font.family'] = 'NanumGothic'
plt.rcParams['axes.unicode_minus'] = False

# 데이터 로드
df = pd.read_csv('data/project_welding.csv')

print("[Step 2-1] 데이터 기본 정보")
print(f"전체 데이터: {len(df)}행, {len(df.columns)}열")
print(f"컬럼: {list(df.columns)}")

print("\n[Step 2-2] 기술통계량")
print(df.describe())

print("\n[Step 2-3] 불량률 분석")
print(f"불량률: {df['quality'].mean():.2%}")
print(f"- 정상(0): {(df['quality']==0).sum()}개")
print(f"- 불량(1): {(df['quality']==1).sum()}개")

# 시각화: 품질별 용접전류 분포
plt.figure(figsize=(10, 4))
plt.subplot(1, 2, 1)
df[df['quality']==0]['current'].hist(alpha=0.7, label='정상', bins=20)
df[df['quality']==1]['current'].hist(alpha=0.7, label='불량', bins=20)
plt.xlabel('용접전류 (A)')
plt.ylabel('빈도')
plt.title('품질별 용접전류 분포')
plt.legend()

plt.subplot(1, 2, 2)
df[df['quality']==0]['voltage'].hist(alpha=0.7, label='정상', bins=20)
df[df['quality']==1]['voltage'].hist(alpha=0.7, label='불량', bins=20)
plt.xlabel('전압 (V)')
plt.ylabel('빈도')
plt.title('품질별 전압 분포')
plt.legend()

plt.tight_layout()
plt.savefig('eda_distribution.png', dpi=150)
plt.show()
print("\n시각화 저장: eda_distribution.png")
```

**[시나리오 2-1]**
> 품질관리팀에서 **"불량률이 15%를 초과하면 생산라인 점검이 필요하다"**는 기준을 제시했습니다. 현재 데이터를 분석하여 라인 점검 필요 여부를 판단해야 합니다.

**과제:**
1. 코드를 실행하여 현재 불량률을 확인하세요
2. **라인 점검 필요 여부**를 판단하고 근거를 제시하세요
3. 판단 결과를 시각적으로 보여줄 수 있도록 **코드를 수정**하세요:
   - 불량률을 막대그래프로 표현
   - 15% 기준선을 빨간 점선으로 추가
4. 아래 양식으로 제출하세요

**[제출 양식]**
```
■ 현재 불량률: _____%

■ 라인 점검 필요 여부: (필요함 / 필요없음)
  - 판단 근거: (1~2문장)

■ 추가한 시각화 코드:
(코드 붙여넣기)

■ 실행 결과 스크린샷: (이미지 첨부)
```

---

**[시나리오 2-2]**
> 생산기술팀에서 "와이어송급속도(wire_feed_speed)와 입열량(heat_input)도 분석해달라"고 요청했습니다. 현재 EDA 코드는 용접전류와 전압만 시각화하고 있습니다.

**과제:**
1. 기존 시각화 코드를 수정하여 **4개 변수**(current, voltage, wire_feed_speed, heat_input)를 모두 표현하는 **2x2 서브플롯**을 만드세요
2. 4개 변수 중 **불량 예측에 가장 유용해 보이는 변수**를 선택하고 이유를 설명하세요
3. 아래 양식으로 제출하세요

**[제출 양식]**
```
■ 수정된 시각화 코드 전체:
(코드 붙여넣기)

■ 새로운 시각화 결과: (스크린샷 첨부)

■ 가장 유용한 변수: _____________
  - 선택 이유: (2~3문장)
```

---

### Step 3: 데이터 전처리 (15점)

```python
# ========================================
# Step 3: 데이터 전처리
# ========================================

from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler

print("[Step 3-1] 특성과 타겟 분리")
feature_cols = ['current', 'voltage', 'wire_feed_speed', 'travel_speed',
                'gas_flow', 'arc_length', 'heat_input']
X = df[feature_cols]
y = df['quality']
print(f"특성 (X): {X.shape}")
print(f"타겟 (Y): {y.shape}")

# ============================================
# TODO: test_size 값을 변경해보세요 (0.1 ~ 0.3)
# 기본값: 0.2 (20%를 테스트용으로 사용)
# ============================================
TEST_SIZE = 0.2  # TODO: 이 값을 0.1 또는 0.3으로 변경해보세요

print(f"\n[Step 3-2] 학습/테스트 분할 (test_size={TEST_SIZE})")
X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=TEST_SIZE, random_state=42, stratify=y
)
print(f"학습 데이터: {len(X_train)}개")
print(f"테스트 데이터: {len(X_test)}개")

print("\n[Step 3-3] 정규화 (StandardScaler)")
scaler = StandardScaler()
X_train_scaled = scaler.fit_transform(X_train)
X_test_scaled = scaler.transform(X_test)
print("정규화 완료!")
print(f"정규화 후 학습 데이터 평균: {X_train_scaled.mean():.4f}")
print(f"정규화 후 학습 데이터 표준편차: {X_train_scaled.std():.4f}")
```

**[시나리오 3-1]**
> QA팀에서 **"테스트 데이터가 너무 적어서 모델 신뢰도를 검증하기 어렵다"**고 피드백했습니다. 현재 test_size=0.2인데, 적절한 비율을 찾아 제안해야 합니다.

**과제:**
1. test_size를 **0.1, 0.2, 0.3**으로 변경하며 실험하는 **반복문 코드**를 작성하세요
2. 각 설정에서의 학습/테스트 데이터 개수와 **Step 5에서의 최종 모델 성능**을 비교하세요
3. 최종 권장 비율을 선택하고, **QA팀에 보낼 1문단 설명**을 작성하세요
4. 아래 양식으로 제출하세요

**[제출 양식]**
```
■ 실험 코드 (반복문):
(코드 붙여넣기)

■ 결과 비교표:
| test_size | 학습 데이터 | 테스트 데이터 | 테스트 정확도 |
|-----------|------------|--------------|--------------|
| 0.1       |            |              |              |
| 0.2       |            |              |              |
| 0.3       |            |              |              |

■ 최종 권장 비율: _____

■ QA팀 설명문:
(1문단으로 작성 - 왜 이 비율을 권장하는지, 학습 데이터와 테스트 데이터의 균형에 대한 설명 포함)
```

---

### Step 4: 모델 학습 (20점)

```python
# ========================================
# Step 4: 모델 학습
# ========================================

from sklearn.ensemble import RandomForestClassifier

# ============================================
# TODO: 하이퍼파라미터를 변경해보세요
# n_estimators: 트리 개수 (50, 100, 200)
# max_depth: 트리 최대 깊이 (3, 5, 10, None)
# ============================================
N_ESTIMATORS = 100  # TODO: 50 또는 200으로 변경해보세요
MAX_DEPTH = 5       # TODO: 3, 10, None으로 변경해보세요

print(f"[Step 4] 모델 학습")
print(f"하이퍼파라미터: n_estimators={N_ESTIMATORS}, max_depth={MAX_DEPTH}")

model = RandomForestClassifier(
    n_estimators=N_ESTIMATORS,
    max_depth=MAX_DEPTH,
    random_state=42
)
model.fit(X_train_scaled, y_train)
print("RandomForest 모델 학습 완료!")

# 학습 정확도
train_accuracy = model.score(X_train_scaled, y_train)
print(f"\n학습 정확도: {train_accuracy:.2%}")
```

**[시나리오 4-1]**
> IT인프라팀에서 **"모델 학습에 시간이 너무 오래 걸린다"**고 불만을 제기했습니다. n_estimators가 높을수록 정확하지만 느려집니다. **정확도와 학습 시간의 균형점**을 찾아야 합니다.

**과제:**
1. n_estimators를 **[50, 100, 150, 200]**으로 변경하며 **학습 시간**과 **정확도**를 측정하는 코드를 작성하세요 (힌트: `import time`)
2. 결과를 **시간-정확도 그래프**로 시각화하세요
3. 권장 설정값을 선택하고, **IT팀과 품질팀 모두를 설득할 수 있는 근거**를 작성하세요
4. 아래 양식으로 제출하세요

**[제출 양식]**
```
■ 실험 코드 (시간 측정 포함):
(코드 붙여넣기)

■ 실험 결과표:
| n_estimators | 학습 시간(초) | 학습 정확도 | 테스트 정확도 |
|--------------|--------------|------------|--------------|
| 50           |              |            |              |
| 100          |              |            |              |
| 150          |              |            |              |
| 200          |              |            |              |

■ 시간-정확도 그래프: (스크린샷 첨부)

■ 권장 설정: n_estimators = _____
  - IT팀 설득 근거: (학습 시간 관점)
  - 품질팀 설득 근거: (정확도 관점)
```

---

**[시나리오 4-2]**
> 현재 코드는 하이퍼파라미터를 수동으로 변경해야 합니다. 자동으로 최적 조합을 찾는 **GridSearchCV** 코드를 추가하세요.

**과제:**
1. `sklearn.model_selection.GridSearchCV`를 사용하여 아래 파라미터 조합을 탐색하세요:
   - n_estimators: [50, 100, 200]
   - max_depth: [3, 5, 10]
2. 최적 파라미터 조합과 교차검증 점수를 출력하세요
3. 아래 양식으로 제출하세요

**[제출 양식]**
```
■ GridSearchCV 코드:
(코드 붙여넣기)

■ 실행 결과:
- 최적 파라미터: n_estimators=____, max_depth=____
- 교차검증 점수: _____%

■ (선택) 전체 탐색 결과표: (cv_results_를 표로 정리)
```

---

### Step 5: 모델 평가 (15점)

```python
# ========================================
# Step 5: 모델 평가
# ========================================

from sklearn.metrics import accuracy_score, classification_report, confusion_matrix

print("[Step 5-1] 테스트 데이터 예측")
y_pred = model.predict(X_test_scaled)

print("\n[Step 5-2] 정확도")
accuracy = accuracy_score(y_test, y_pred)
print(f"테스트 정확도: {accuracy:.2%}")

print("\n[Step 5-3] 분류 보고서")
print(classification_report(y_test, y_pred, target_names=['정상', '불량']))

print("\n[Step 5-4] 혼동행렬")
cm = confusion_matrix(y_test, y_pred)
print(cm)

print("\n[Step 5-5] 특성 중요도")
importances = model.feature_importances_
for name, imp in sorted(zip(feature_cols, importances), key=lambda x: x[1], reverse=True):
    print(f"  {name}: {imp:.3f}")

# 특성 중요도 시각화
plt.figure(figsize=(10, 5))
plt.barh(feature_cols, importances, color='steelblue')
plt.xlabel('중요도')
plt.title('용접 품질 예측 - 특성 중요도')
plt.tight_layout()
plt.savefig('feature_importance.png', dpi=150)
plt.show()
print("\n특성 중요도 그래프 저장: feature_importance.png")
```

**[시나리오 5-1]**
> 경영진 보고에서 **"불량품을 정상으로 잘못 판단하면(False Negative) 고객 클레임으로 이어져 손해가 크다"**는 피드백이 있었습니다. 정확도(Accuracy)보다 **재현율(Recall)**이 중요합니다.

**과제:**
1. 현재 모델의 혼동행렬에서 **FN(불량→정상 오판) 개수**를 확인하세요
2. **재현율이 95% 이상**이 되도록 모델을 조정하세요
   - 힌트 1: `class_weight='balanced'` 파라미터 사용
   - 힌트 2: 예측 임계값(threshold) 조정 - `model.predict_proba()` 활용
3. 조정 전/후 성능을 비교하는 표를 작성하세요
4. 아래 양식으로 제출하세요

**[제출 양식]**
```
■ 현재 모델 FN 개수: _____개

■ 재현율 개선을 위한 수정된 코드:
(코드 붙여넣기)

■ 성능 비교표:
| 지표 | 조정 전 | 조정 후 |
|------|--------|--------|
| 정확도 (Accuracy) | | |
| 정밀도 (Precision) | | |
| 재현율 (Recall) | | |
| F1-score | | |

■ 재현율 95% 달성 여부: (달성 / 미달성)
  - 조정 방법 설명: (1~2문장)
```

---

**[시나리오 5-2]**
> 생산기술팀에서 **"어떤 공정 변수를 중점 관리해야 하는지 알려달라"**고 요청했습니다.

**과제:**
1. 특성 중요도 분석 결과를 바탕으로 **중점 관리 대상 변수 3개**를 선정하세요
2. 각 변수에 대해 **현장에서 어떻게 관리해야 하는지** 구체적 조언을 작성하세요
3. 특성 중요도 그래프에 **관리 우선순위를 표시**하도록 코드를 수정하세요 (예: 상위 3개 막대 색상 강조)
4. 아래 양식으로 제출하세요

**[제출 양식]**
```
■ 중점 관리 변수 3개:
| 순위 | 변수명 | 중요도 | 현장 관리 방안 |
|------|--------|--------|---------------|
| 1 | | | (구체적 조언) |
| 2 | | | (구체적 조언) |
| 3 | | | (구체적 조언) |

■ 수정된 시각화 코드 (색상 강조):
(코드 붙여넣기)

■ 수정된 그래프: (스크린샷 첨부)
```

---

### Step 6: 서비스 배포 (25점)

아래 코드를 `app.py` 파일로 저장한 후 실행하세요.

```python
# ========================================
# Step 6: Streamlit 품질 예측 웹앱
# ========================================
# 파일명: app.py
# 실행: streamlit run app.py

import streamlit as st
import pandas as pd
import numpy as np
from sklearn.ensemble import RandomForestClassifier
from sklearn.preprocessing import StandardScaler

st.set_page_config(page_title="용접 품질 예측", layout="wide")
st.title("KAMP 용접 품질 예측 시스템")
st.caption("27차시 ML 모델 서비스 배포 실습")

# 모델 학습 (실제 환경에서는 저장된 모델을 로드)
@st.cache_resource
def load_model():
    df = pd.read_csv('data/project_welding.csv')
    feature_cols = ['current', 'voltage', 'wire_feed_speed', 'travel_speed',
                    'gas_flow', 'arc_length', 'heat_input']
    X = df[feature_cols]
    y = df['quality']

    scaler = StandardScaler()
    X_scaled = scaler.fit_transform(X)

    model = RandomForestClassifier(n_estimators=100, max_depth=5, random_state=42)
    model.fit(X_scaled, y)

    return model, scaler, feature_cols

model, scaler, feature_cols = load_model()

# 사이드바: 센서 입력
st.sidebar.header("센서 입력값")

# ============================================
# TODO: 슬라이더 범위를 변경해보세요
# 예: 용접전류 범위를 (150, 250)으로 좁혀보세요
# ============================================
current = st.sidebar.slider("용접전류 (A)", 100, 300, 200)
voltage = st.sidebar.slider("전압 (V)", 15, 35, 25)
wire_feed_speed = st.sidebar.slider("와이어송급속도 (m/min)", 3.0, 15.0, 8.0)
travel_speed = st.sidebar.slider("용접속도 (cm/min)", 20, 80, 50)
gas_flow = st.sidebar.slider("가스유량 (L/min)", 10, 30, 20)
arc_length = st.sidebar.slider("아크길이 (mm)", 2.0, 8.0, 5.0)
heat_input = st.sidebar.slider("입열량 (kJ/cm)", 0.5, 3.0, 1.5)

# 예측 버튼
if st.button("품질 예측", type="primary"):
    # 입력 데이터 구성
    input_data = pd.DataFrame({
        'current': [current],
        'voltage': [voltage],
        'wire_feed_speed': [wire_feed_speed],
        'travel_speed': [travel_speed],
        'gas_flow': [gas_flow],
        'arc_length': [arc_length],
        'heat_input': [heat_input]
    })

    # 정규화 및 예측
    input_scaled = scaler.transform(input_data)
    prediction = model.predict(input_scaled)[0]
    probability = model.predict_proba(input_scaled)[0]

    # 결과 표시
    st.header("예측 결과")
    col1, col2 = st.columns(2)

    with col1:
        if prediction == 1:
            st.error(f"불량 예측 (확률: {probability[1]:.1%})")
        else:
            st.success(f"정상 예측 (확률: {probability[0]:.1%})")

    with col2:
        st.metric("불량 확률", f"{probability[1]:.1%}")

    # 입력 데이터 표시
    st.subheader("입력 데이터")
    st.dataframe(input_data.T.rename(columns={0: '값'}))

st.markdown("---")
st.info("이 앱은 KAMP 용접 데이터 기반 품질 예측 모델을 사용합니다.")
```

**[실행 방법]**
1. 위 코드를 `app.py` 파일로 저장
2. 터미널에서 `streamlit run app.py` 실행
3. 브라우저에서 http://localhost:8501 접속
4. 센서 값을 조절하고 "품질 예측" 버튼 클릭

---

**[시나리오 6-1]**
> 현장 용접기의 실제 스펙을 확인한 결과, 다음과 같은 제약이 있습니다:
> - 용접전류: **150~250A** (기존 100~300A)
> - 전압: **20~30V** (기존 15~35V)
> - 가스유량: **15~25 L/min** (기존 10~30)

**과제:**
1. `app.py`의 슬라이더 범위를 **실제 장비 스펙**에 맞게 수정하세요
2. 기본값도 각 범위의 **중간값**으로 조정하세요
3. 아래 양식으로 제출하세요

**[제출 양식]**
```
■ 수정된 슬라이더 코드 (3개 변수):

# 수정 전
current = st.sidebar.slider("용접전류 (A)", 100, 300, 200)
voltage = st.sidebar.slider("전압 (V)", 15, 35, 25)
gas_flow = st.sidebar.slider("가스유량 (L/min)", 10, 30, 20)

# 수정 후
(코드 붙여넣기)
```

---

**[시나리오 6-2]**
> 현장 작업자가 **"예측 결과만 보이고, 왜 불량인지 모르겠다"**고 피드백했습니다. **예측 근거**를 보여주는 기능을 추가해야 합니다.

**과제 (아래 중 택1):**

**옵션 A:** 현재 입력값이 정상 범위인지 표시
- 예: "용접전류 280A → 정상 범위(150-250A) 초과!"
- 정상 범위를 벗어난 입력값에 경고 메시지 표시

**옵션 B:** 특성 중요도 상위 3개를 함께 표시
- 모델의 feature_importances_를 활용
- 어떤 변수가 예측에 가장 큰 영향을 미치는지 표시

**[제출 양식]**
```
■ 선택한 옵션: (A 또는 B)

■ 추가한 코드:
(코드 붙여넣기)

■ 실행 스크린샷: (기능이 동작하는 화면 캡처)
```

---

**[시나리오 6-3]**
> 품질팀에서 **"예측 기록을 저장해서 나중에 분석하고 싶다"**고 요청했습니다.

**과제:**
1. 예측 버튼 클릭 시 **예측 기록을 CSV 파일로 저장**하는 기능을 추가하세요
2. 저장 내용: 입력값 7개 + 예측 결과 + 불량 확률 + 예측 시간
3. 아래 양식으로 제출하세요

**[제출 양식]**
```
■ CSV 저장 기능 코드:
(코드 붙여넣기)

■ 저장된 CSV 파일 예시 (처음 3행):
| current | voltage | ... | prediction | probability | timestamp |
|---------|---------|-----|------------|-------------|-----------|
| | | | | | |
```

---

### [종합 보고서] (가산점 +10점)

**[최종 시나리오]**
> 프로젝트가 완료되어 **경영진에게 최종 보고**를 해야 합니다.

아래 내용을 포함하여 **500자 이상**의 보고서를 작성하세요.

**필수 포함 내용:**
1. **프로젝트 개요**: 해결하려는 문제와 접근 방식
2. **데이터 분석 결과**: 발견한 주요 인사이트 (불량률, 주요 변수 등)
3. **모델 성능**: 최종 성능 지표와 비즈니스 영향
4. **배포 결과**: 구현한 기능과 현장 피드백 반영 사항
5. **향후 계획**: 추가 개선이 필요한 부분

**[종합 보고서 작성란]**
```
(여기에 500자 이상 작성)



```

---

### 채점 기준 (초급)

| 항목 | 배점 | 평가 기준 |
|------|------|----------|
| Step 1: 문제 정의 | 10점 | 시스템 개요서 완성도 |
| Step 2: EDA | 15점 | 시각화 코드 수정 + 분석 품질 |
| Step 3: 전처리 | 15점 | 실험 코드 + 권장안 논리성 |
| Step 4: 모델링 | 20점 | 시간 측정 + GridSearch 구현 |
| Step 5: 평가 | 15점 | 재현율 개선 + 관리 제안서 |
| Step 6: 배포 | 25점 | UI 커스터마이징 + 기능 추가 |
| **합계** | **100점** | |
| [종합 보고서] | **+10점** | 500자 이상, 논리적 구성 |

**※ 제출산출물:** `27차시_초급_프로젝트_제출자이름.ipynb` + `app.py` + 스크린샷

---

### [정답 예시: 심사 참고용 - 학습자에게 제시하지 않음]

```
Step 2: 불량률 10% (15% 미만이므로 라인 점검 불필요)
Step 3: 학습 데이터 400개 (test_size=0.2일 때)
        정규화 후 평균 0.0, 표준편차 1.0
Step 4: 학습 정확도 약 97~99%
        GridSearch 최적: n_estimators=100, max_depth=5
Step 5: 테스트 정확도 약 94%
        가장 중요한 특성: current, voltage, heat_input
        class_weight='balanced' 사용 시 재현율 향상
Step 6: 용접전류 150A → 정상 예측 가능성 높음
        용접전류 250A → 불량 예측 가능성 높음
```

---

## <중급난이도> 제조 품질 예측 서비스 직접 설계

**제출의도:**
최소한의 가이드라인만 제공하고, 전체 ML 파이프라인을 직접 설계하고 구현하는 능력을 평가합니다.

**제출내용:**
아래 요구사항 체크리스트를 참고하여 **직접 코드를 작성**하고, 각 단계의 결과를 문서화하세요.
(코드 템플릿이 제공되지 않습니다)

---

### Step 1: 문제 정의 (10점)

**요구사항:**
- [ ] 비즈니스 문제를 명확히 정의하세요 (어떤 문제를 해결하려 하는가?)
- [ ] 입력 변수(X)와 출력 변수(Y)를 정의하세요
- [ ] 성공 기준을 수치로 정의하세요 (예: 정확도 90% 이상)

**산출물:** 문제 정의서 (마크다운 셀)

---

### Step 2: 탐색적 데이터 분석 (15점)

**요구사항:**
- [ ] `data/project_welding.csv` 데이터를 로드하세요
- [ ] 데이터 기본 정보를 확인하세요 (행/열 수, 컬럼명, 데이터 타입)
- [ ] 기술통계량을 출력하세요 (df.describe())
- [ ] 타겟 변수(quality)의 분포를 확인하세요 (불량률)
- [ ] 시각화를 2개 이상 작성하세요 (히스토그램, 박스플롯, 상관관계 등)

**산출물:** EDA 결과 및 인사이트 (200자 이상)

---

### Step 3: 데이터 전처리 (15점)

**요구사항:**
- [ ] 특성(X)과 타겟(Y)을 분리하세요
- [ ] train_test_split으로 학습/테스트 데이터를 분할하세요
- [ ] StandardScaler 또는 MinMaxScaler로 정규화를 수행하세요
- [ ] 전처리 과정을 설명하세요

**산출물:** 전처리된 데이터셋 (X_train, X_test, y_train, y_test)

---

### Step 4: 모델링 (25점)

**요구사항:**
- [ ] 2개 이상의 모델을 학습하세요 (예: LogisticRegression, RandomForest, GradientBoosting 등)
- [ ] 각 모델에 대해 하이퍼파라미터 튜닝을 시도하세요
- [ ] 모델 비교표를 작성하세요

**비교표 예시:**

| 모델 | 하이퍼파라미터 | 학습 정확도 | 테스트 정확도 |
|------|--------------|-----------|-------------|
| LogisticRegression | C=1.0 | ??% | ??% |
| RandomForest | n=100, d=5 | ??% | ??% |
| ... | ... | ... | ... |

**산출물:** 모델 비교표 + 최적 모델 선택 근거

---

### Step 5: 모델 평가 (15점)

**요구사항:**
- [ ] 최종 모델의 혼동행렬을 출력하세요
- [ ] 분류 보고서(precision, recall, f1-score)를 출력하세요
- [ ] 특성 중요도를 시각화하세요 (트리 기반 모델인 경우)
- [ ] 최종 모델 선택 근거를 작성하세요 (200자 이상)

**산출물:** 평가 결과 + 최종 모델 선택 근거

---

### Step 6: 서비스 배포 (20점)

**옵션 중 택 1:**

#### 옵션 A: Streamlit 웹앱 구현
- [ ] 센서 입력을 받는 인터페이스 구현 (슬라이더 또는 숫자 입력)
- [ ] 예측 버튼 클릭 시 결과 표시
- [ ] 예측 확률 표시
- [ ] `streamlit run app.py`로 실행 가능

#### 옵션 B: FastAPI REST API 구현
- [ ] `/predict` POST 엔드포인트 구현
- [ ] Pydantic 모델로 입력 데이터 검증
- [ ] JSON 응답 반환 (prediction, probability)
- [ ] `uvicorn main:app`으로 실행 가능
- [ ] curl 또는 requests로 테스트 결과 첨부

**(보너스 +10점)** 옵션 A와 B를 모두 구현하면 추가 10점 부여

**산출물:**
- 옵션 A 선택 시: `app.py` + 실행 스크린샷
- 옵션 B 선택 시: `main.py` + API 테스트 결과 (curl/requests)
- 둘 다 구현 시: 모든 파일 + 스크린샷/테스트 결과

---

### [종합 보고서] (필수 - 800자 이상)

직접 설계한 ML 파이프라인의 전체 과정을 정리하고, **800자 이상**으로 서술하세요.

**필수 포함 내용:**
1. **프로젝트 개요**: 해결하려는 문제와 접근 방식
2. **데이터 분석 인사이트**: EDA에서 발견한 주요 패턴이나 특이점
3. **모델 선택 및 튜닝 과정**: 왜 해당 모델을 선택했고, 하이퍼파라미터는 어떻게 결정했는가?
4. **최종 성능과 개선 방향**: 목표 성능을 달성했는가? 추가로 개선할 수 있는 방법은?
5. **배포 과정에서의 고려사항**: 서비스화 시 어떤 점을 고려했는가?

**[종합 보고서 작성란]**
```
(여기에 800자 이상 작성)




```

> **주의**: 종합 보고서 미제출 시 **-20점 감점**됩니다.

---

### 채점 기준 (중급)

| 항목 | 배점 | 세부 기준 |
|------|------|----------|
| Step 1: 문제 정의 | 10점 | 문제/변수/성공기준 명확히 정의 |
| Step 2: EDA | 15점 | 기본정보 + 통계량 + 시각화 2개 이상 |
| Step 3: 전처리 | 15점 | 분리/분할/정규화 완료 |
| Step 4: 모델링 | 25점 | 2개 이상 모델 + 튜닝 + 비교표 |
| Step 5: 평가 | 15점 | 혼동행렬/보고서/중요도 + 선택근거 |
| Step 6: 배포 | 20점 | 택1 완성 (A 또는 B) |
| [종합 보고서] | **필수** | 800자 이상 서술 (미제출 시 -20점) |
| (보너스) | +10점 | A와 B 모두 완성 |
| **합계** | **100점** (최대 110점) | |

**※ 제출산출물:** `27차시_중급_프로젝트_제출자이름.ipynb` + 배포 코드 파일 + 스크린샷

---

## 첨부 파일 안내

학습자에게 다음 파일을 제공합니다:

1. **데이터 파일 (KAMP 제조AI데이터셋 스펙 기반 합성 데이터)**:
   - `data/part1_injection_molding.csv` - Part 1: 사출성형기 품질 데이터 (300행)
   - `data/part2_cnc_tool_wear.csv` - Part 2: CNC 공구마모 데이터 (450행)
   - `data/part3_press_quality.csv` - Part 3: 프레스기 품질 데이터 (600행)
   - `data/project_welding.csv` - 프로젝트: 용접 품질 데이터 (500행)

2. **참고 자료**: 각 차시의 강의 슬라이드 및 실습 노트북

> **참고**: 데이터는 KAMP(한국AI제조플랫폼) 가이드북의 실제 제조 변수명 및 값 범위를 기반으로 생성된 합성 데이터입니다.

---

## 채점 기준 요약

| 난이도 | 특징 | 총점 |
|--------|------|------|
| **초급** | 시나리오 기반 + 코드 수정 + 분석 보고서 + 종합보고서(가산점) | 100점 (+10 가산) |
| **중급** | 요구사항만 제공 + 직접 설계/구현 + 종합보고서(필수) | 100점 (+10 보너스) |

---

*본 과제는 "제조데이터를 활용한 AI 이해와 예측 모델 구축" 과정의 종합 실습 프로젝트입니다.*
